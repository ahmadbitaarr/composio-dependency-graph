# Raw Tool Schema Observations

Generated by `bun run fetch:tools` at 2026-07-20T14:29:49.818Z.

## Retrieval verification

| Toolkit | Tools | Pages | Pagination required | SDK cross-check | Toolkit version(s) | Metadata version | Raw file size |
|---|---:|---:|---|---|---|---|---:|
| googlesuper | 467 | 1 | No | MATCH (467) | 20260714_00 | 20260714_00 | 6.77 MiB |
| github | 893 | 1 | No | MATCH (893) | 20260713_00 | 20260713_00 | 10.43 MiB |

The raw JSON files preserve every parsed field returned by the toolkit metadata endpoint and every page returned by the tools endpoint. API page objects are stored unchanged inside `tool_list_responses`; the surrounding snapshot metadata records the request that produced them.

## What a raw tool definition contains

Across the inspected definitions, a tool can contain its slug, name, description, toolkit metadata, input schema, output schema, tags, OAuth scopes, current version, available versions, no-auth status, and deprecation metadata. Input and output schemas are usually JSON Schema objects with `type`, `properties`, `required`, nested objects/arrays, `$ref`, and `$defs`, but the script also detects flat parameter maps, scalar/array schemas, empty schemas, and unknown shapes.

## Output schema quality

The classifications below are deterministic schema-quality heuristics, not claims that the underlying APIs always return every field at runtime.

| Toolkit | COMPLETE | PARTIAL | GENERIC | MISSING |
|---|---:|---:|---:|---:|
| googlesuper | 426 | 36 | 5 | 0 |
| github | 729 | 23 | 141 | 0 |

- `COMPLETE`: named resource fields are present with mostly typed and described structure.
- `PARTIAL`: useful structure exists, but some nested shape, typing, or descriptions are incomplete.
- `GENERIC`: only a generic envelope or open-ended object is available, without reliable resource paths.
- `MISSING`: no declared output schema is present.

## Schema format counts

### googlesuper

| Format | Inputs | Outputs |
|---|---:|---:|
| STANDARD_JSON_SCHEMA | 467 | 467 |
| FLAT_PARAMETER_MAP | 0 | 0 |
| SCALAR_OR_ARRAY | 0 | 0 |
| EMPTY | 0 | 0 |
| UNKNOWN | 0 | 0 |

### github

| Format | Inputs | Outputs |
|---|---:|---:|
| STANDARD_JSON_SCHEMA | 893 | 893 |
| FLAT_PARAMETER_MAP | 0 | 0 |
| SCALAR_OR_ARRAY | 0 | 0 |
| EMPTY | 0 | 0 |
| UNKNOWN | 0 | 0 |

## Representative schema examples

### googlesuper

#### Gmail messages

##### `GOOGLESUPER_LIST_MESSAGES` — COMPLETE

- Description: DEPRECATED: Use GMAIL_FETCH_EMAILS instead. Lists the messages in the user's mailbox. Use when you need to retrieve a list of email messages with optional filtering by labels or search query.
- Required inputs: None declared
- Optional inputs: `q` (string) — Only return messages matching the specified query. Supports the same query format as the Gmail search box. For example, 'from:someuser@example.com is:unread'. Cannot be used when accessing the API using the gmail.metadata scope.; `user_id` (string) — The user's email address or 'me' to specify the authenticated user.; `label_ids` (array) — Only return messages with labels that match all of the specified label IDs. Messages in a thread might have labels that other messages in the same thread don't have.; `page_token` (string) — Page token to retrieve a specific page of results in the list.; `max_results` (integer) — Maximum number of messages to return. Defaults to 100. The maximum allowed value is 500.; `include_spam_trash` (boolean) — Include messages from SPAM and TRASH in the results. Default is false.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.messages` (array) — List of messages. Each message resource contains only an id and a threadId. Additional message details can be fetched using the messages.get method.; `data.messages[].id` (string) — The immutable ID of the message.; `data.messages[].threadId` (string) — The ID of the thread the message belongs to.; `data.nextPageToken` (string) — Token to retrieve the next page of results in the list.; `data.resultSizeEstimate` (integer) — Estimated total number of results.; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: yes; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.readonly

##### `GOOGLESUPER_FETCH_MESSAGE_BY_MESSAGE_ID` — COMPLETE

- Description: Fetches a specific email message by its ID, provided the `message_id` exists and is accessible to the authenticated `user_id`. Spam/trash messages are excluded unless upstream list/search calls used `include_spam_trash=true`. Use `internalDate` (milliseconds since epoch) rather than header `Date` for recency checks.
- Required inputs: `message_id` (string) — The Gmail API message ID (hexadecimal string, typically 15-16 characters like '19b11732c1b578fd'). Must be obtained from Gmail API responses (e.g., List Messages, Search Messages). Do NOT use email subjects, dates, sender names, or custom identifiers. Do NOT use `threadId` (use GMAIL_FETCH_MESSAGE_BY_THREAD_ID for threads), the Message-ID email header, or any fabricated value — only IDs from Gmail API list/search responses.
- Optional inputs: `format` (string) — Format for message content. 'minimal': lightest (ID, thread ID, labels only). 'metadata': headers and message metadata without body content - ideal for summarization, analysis, or when you only need subject/sender/timestamp (recommended for most use cases). 'full': complete MIME structure with 50+ headers, nested parts, and base64url-encoded body data - heavy payload, only use when you need the complete raw MIME structure for parsing attachments or body content. 'raw': entire RFC 2822 formatted message as base64url string.; `user_id` (string) — User's email address or 'me' for the authenticated user.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.to` (string) — The recipient of the message; `data.sender` (string) — The sender of the message; `data.payload` (object) — The payload of the message; `data.preview` (object) — The preview of the message; `data.subject` (string) — The subject of the message; `data.labelIds` (array) — The label IDs of the message; `data.threadId` (string) — The thread ID of the message; `data.messageId` (string) — The message ID of the message; `data.display_url` (string) — Web UI link for this resource - clickable URL pointing to the Gmail web interface; `data.messageText` (string) — The text of the message; `data.attachmentList` (array) — The list of attachments; … 3 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.readonly

##### `GOOGLESUPER_SEND_EMAIL` — COMPLETE

- Description: Sends an email via Gmail API using the authenticated user's Google profile display name. Sends immediately and is irreversible — confirm recipients, subject, body, and attachments before calling. At least one of 'to' (or 'recipient_email'), 'cc', or 'bcc' must be provided. At least one of subject or body must be provided. Requires `is_html=True` if the body contains HTML. All common file types including PNG, JPG, PDF, MP4, etc. are supported as attachments. Gmail API limits total message size to ~25 MB after base64 encoding. To reply in an existing thread, use GMAIL_REPLY_TO_THREAD instead. No scheduled send support; enforce timing externally.
- Required inputs: None declared
- Optional inputs: `cc` (array) — Carbon Copy (CC) recipients' email addresses. At least one of 'to'/'recipient_email', 'cc', or 'bcc' must be provided.; `bcc` (array) — Blind Carbon Copy (BCC) recipients' email addresses. At least one of 'to'/'recipient_email', 'cc', or 'bcc' must be provided.; `body` (string) — Email content (plain text or HTML). Either subject or body must be provided for the email to be sent. If HTML, `is_html` must be `True`.; `is_html` (boolean) — Set to `True` if the email body contains HTML tags.; `subject` (string) — Subject line of the email. Either subject or body must be provided for the email to be sent.; `user_id` (string) — User's email address; the literal 'me' refers to the authenticated user.; `attachment` (unknown) — File(s) to attach. Accepts a single file or a list of files. IMPORTANT: mimetype MUST contain a '/' separator - single words like 'pdf' or 'new' are invalid. Gmail API limits: total message size must not exceed ~25 MB after base64 encoding. Omit or set to null for no attachment. Empty attachment objects (with all fields empty/whitespace) are treated as no attachment.; `from_email` (string) — Sender email address for the 'From' header. Use this to send from a verified alias configured in Gmail's 'Send mail as' settings. When not provided, the authenticated user's primary email address is used. The alias must be verified in Gmail settings before use.; `recipient_email` (string) — Primary recipient's email address. You can also use 'to' as an alias for this parameter. At least one of 'to'/'recipient_email', 'cc', or 'bcc' must be provided. Use extra_recipients if you want to send to multiple recipients. Use the special value 'me' to send to your own authenticated email address. Must be a full user@domain address; 'me' is not valid here and will fail.; `extra_recipients` (array) — Additional 'To' recipients' email addresses (not Cc or Bcc). Should only be used if recipient_email is also provided.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (string) — The immutable ID of the sent message.; `data.raw` (string) — The entire email message in RFC 2822 format, base64url-encoded.; `data.payload` (object) — The parsed email structure, including headers and body parts.; `data.snippet` (string) — A short extract of the message text.; `data.labelIds` (array) — List of IDs of labels applied to this message.; `data.threadId` (string) — The ID of the thread the message belongs to.; `data.historyId` (string) — The ID of the last history record that modified this message.; `data.display_url` (string) — Web UI link for this resource - set by each action's execute().; `data.internalDate` (string) — The internal timestamp of the message in milliseconds since epoch.; `data.sizeEstimate` (integer) — Estimated size of the message in bytes.; `error` (string) — Error if any occurred during the execution of the action; … 1 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.compose, https://www.googleapis.com/auth/gmail.send

#### Gmail threads

##### `GOOGLESUPER_LIST_THREADS` — COMPLETE

- Description: Retrieves a list of email threads from a Gmail account, identified by `user_id` (email address or 'me'), supporting filtering and pagination. Spam and trash are excluded by default unless explicitly targeted via `label:spam` or `label:trash` in the query.
- Required inputs: None declared
- Optional inputs: `query` (string) — Filter for threads, using Gmail search query syntax (e.g., 'from:user@example.com is:unread'). Supported operators include `from:`, `to:`, `subject:`, `label:`, `is:unread`, `has:attachment`, `after:`, `before:`. Dates must use `YYYY/MM/DD` format; date operators are UTC-based. Exact subject phrases require quotes (e.g., `subject:'meeting notes'`).; `user_id` (string) — The user's email address or 'me' to specify the authenticated Gmail account.; `verbose` (boolean) — If false, returns threads with basic fields (id, snippet, historyId). If true, returns threads with complete message details including headers, body, attachments, and metadata for each message in the thread. Combining `verbose=true` with large `max_results` produces very large responses; keep `max_results` modest when verbose is enabled.; `page_token` (string) — Token from a previous response to retrieve a specific page of results; omit for the first page.; `max_results` (integer) — Maximum number of threads to return. Hard cap is ~500 per call. For full mailbox coverage, loop using `nextPageToken` via `page_token` until absent.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.threads` (array) — List of email thread resources. Each thread contains an id, and may optionally include historyId and snippet depending on the request.; `data.threads[].id` (string) — Unique identifier of the thread.; `data.threads[].snippet` (string) — A short preview of the thread's message content.; `data.threads[].historyId` (string) — ID of the last history record for the thread.; `data.nextPageToken` (string) — Token for the next page of results; if present, use as page_token in a subsequent request to fetch more threads.; `data.resultSizeEstimate` (integer) — Estimated total threads matching the query (not just this page).; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.readonly

##### `GOOGLESUPER_FETCH_MESSAGE_BY_THREAD_ID` — COMPLETE

- Description: Retrieves messages from a Gmail thread using its `thread_id`, where the thread must be accessible by the specified `user_id`. Returns a `messages` array; `thread_id` is not echoed in the response. Message order is not guaranteed — sort by `internalDate` to find oldest/newest. Check `labelIds` per message to filter drafts. Concurrent bulk calls may trigger 403 `userRateLimitExceeded` or 429; cap concurrency ~10 and use exponential backoff.
- Required inputs: `thread_id` (string) — Hexadecimal thread ID from Gmail API (e.g., '19bf77729bcb3a44'). Obtain from GMAIL_LIST_THREADS or GMAIL_FETCH_EMAILS. Prefixes like 'msg-f:' or 'thread-f:' are auto-stripped. Legacy Gmail web UI IDs (e.g., 'FMfcgzQfBZdVqKZcSVBhqwWLKWCtDdWQ') are NOT supported - use the API thread ID instead. Deduplicate thread_ids before calling when multiple listed messages share the same threadId to avoid redundant calls.
- Optional inputs: `user_id` (string) — The email address of the user.; `page_token` (string) — Opaque page token for fetching a specific page of messages if results are paginated. Iterate calls by passing the returned `nextPageToken` until it is absent; stopping early will miss messages in long threads.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.messages` (array) — List of messages in the thread.; `data.messages[].to` (string) — The recipient of the message; `data.messages[].sender` (string) — The sender of the message; `data.messages[].payload` (object) — The payload of the message; `data.messages[].preview` (object) — The preview of the message; `data.messages[].subject` (string) — The subject of the message; `data.messages[].labelIds` (array) — The label IDs of the message; `data.messages[].threadId` (string) — The thread ID of the message; `data.messages[].messageId` (string) — The message ID of the message; `data.messages[].display_url` (string) — Web UI link for this resource - clickable URL pointing to the Gmail web interface; `data.messages[].messageText` (string) — The text of the message; … 7 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.readonly

##### `GOOGLESUPER_REPLY_TO_THREAD` — COMPLETE

- Description: Sends a reply within a specific Gmail thread using the original thread's subject; do not provide a custom subject as it will start a new conversation instead of replying in-thread. Requires a valid `thread_id` and at least one of `recipient_email`, `cc`, or `bcc`. Supports optional file attachments.
- Required inputs: `thread_id` (string) — Identifier of the Gmail thread for the reply. Must be a valid hexadecimal string, typically 15-16 characters long (e.g., '169eefc8138e68ca'). Prefixes like 'msg-f:' or 'thread-f:' are automatically stripped. Note: Format validation only checks the ID structure; the thread must also exist and be accessible in your Gmail account. Use GMAIL_LIST_THREADS or GMAIL_FETCH_EMAILS to retrieve valid thread IDs. Must be a threadId, not a messageId; passing a messageId can cause the reply to fail or start an unintended new thread.
- Optional inputs: `cc` (array) — Carbon Copy (CC) recipients' email addresses in format 'user@domain.com'. Each address must include both username and domain separated by '@'. At least one of cc, bcc, or recipient_email must be provided.; `bcc` (array) — Blind Carbon Copy (BCC) recipients' email addresses in format 'user@domain.com'. Each address must include both username and domain separated by '@'. At least one of cc, bcc, or recipient_email must be provided.; `is_html` (boolean) — Indicates if `message_body` is HTML; if True, body must be valid HTML, if False, body should not contain HTML tags. Mismatch causes recipients to see raw HTML tags as plain text.; `user_id` (string) — Identifier for the user sending the reply; 'me' refers to the authenticated user.; `attachment` (unknown) — File(s) to attach to the reply. Accepts a single file or a list of files. Total message size including attachments must stay under 25 MB (400 badRequest if exceeded); use Drive links for large files.; `message_body` (string) — Content of the reply message, either plain text or HTML.; `recipient_email` (string) — Primary recipient's email address in format 'user@domain.com'. Must include both username and domain separated by '@'. Required if cc and bcc is not provided, else can be optional. Use extra_recipients if you want to send to multiple recipients.; `extra_recipients` (array) — Additional 'To' recipients' email addresses in format 'user@domain.com' (not Cc or Bcc). Each address must include both username and domain separated by '@'. Should only be used if recipient_email is also provided.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (string) — The immutable ID of the sent message.; `data.raw` (string) — The entire email message in RFC 2822 format, base64url-encoded.; `data.payload` (object) — The parsed email structure, including headers and body parts.; `data.snippet` (string) — A short extract of the message text.; `data.labelIds` (array) — List of IDs of labels applied to this message.; `data.threadId` (string) — The ID of the thread the message belongs to.; `data.historyId` (string) — The ID of the last history record that modified this message.; `data.display_url` (string) — Web UI link for this resource - set by each action's execute().; `data.internalDate` (string) — The internal timestamp of the message in milliseconds since epoch.; `data.sizeEstimate` (integer) — Estimated size of the message in bytes.; `error` (string) — Error if any occurred during the execution of the action; … 1 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://mail.google.com/, https://www.googleapis.com/auth/gmail.compose, https://www.googleapis.com/auth/gmail.modify, https://www.googleapis.com/auth/gmail.send, https://www.googleapis.com/auth/gmail.readonly, https://www.googleapis.com/auth/contacts, https://www.googleapis.com/auth/contacts.other.readonly, https://www.googleapis.com/auth/contacts.readonly

#### Google Contacts or People

##### `GOOGLESUPER_GET_CONTACTS` — COMPLETE

- Description: Fetches contacts (connections) for the authenticated Google account, allowing selection of specific data fields and pagination. Only covers saved contacts and 'Other Contacts'; email-header-only senders are out of scope. Contact records may have sparse data — handle missing fields gracefully. People API shares a per-user QPS quota; HTTP 429 requires exponential backoff (1s, 2s, 4s).
- Required inputs: None declared
- Optional inputs: `page_token` (string) — Token to retrieve a specific page of results, obtained from 'nextPageToken' in a previous response. Repeat calls with each successive `nextPageToken` until it is absent — stopping early silently omits contacts.; `person_fields` (string) — Comma-separated person fields to retrieve for each contact (e.g., 'names,emailAddresses').; `resource_name` (string) — Identifier for the person resource whose connections are listed; use 'people/me' for the authenticated user.; `include_other_contacts` (boolean) — Include 'Other Contacts' (interacted with but not explicitly saved) in addition to regular contacts. WARNING: 'Other Contacts' often have incomplete data - they may lack names, phone numbers, and other fields even when requested. These auto-generated contacts are created from email interactions and typically only have email addresses. Set to False if you need contacts with complete name information. When True, each contact will have a 'contactSource' field indicating its origin. When True, `person_fields` is restricted to `emailAddresses`, `names`, `phoneNumbers`, and `metadata` only — requesting other fields (e.g., `organizations`, `birthdays`) causes validation errors or silent omissions.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.totalItems` (integer) — Total number of connections returned; `data.connections` (array) — List of contact entries; `data.connections[].etag` (string) — Version tag for concurrency control; `data.connections[].urls` (array) — List of the contact's URLs; `data.connections[].urls[].type` (string) — Type of the URL (e.g., blog, homepage); `data.connections[].urls[].value` (string) — The URL string; `data.connections[].urls[].metadata` (object) — Metadata associated with the URL; `data.connections[].urls[].metadata.source` (object) — Origin of the field data; `data.connections[].urls[].metadata.source.id` (string) — Identifier of the source record; `data.connections[].urls[].metadata.source.type` (string) — Type of the source; `data.connections[].urls[].metadata.primary` (boolean) — Whether this field is marked as primary; … 131 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/contacts, https://www.googleapis.com/auth/contacts.readonly, https://www.googleapis.com/auth/contacts.other.readonly

##### `GOOGLESUPER_GET_PEOPLE` — COMPLETE

- Description: Retrieves either a specific person's details (using `resource_name`) or lists 'Other Contacts' (if `other_contacts` is true), with `person_fields` specifying the data to return. Scope is limited to the authenticated user's own contacts and 'Other Contacts' history only.
- Required inputs: None declared
- Optional inputs: `sources` (array) — Source types to include when retrieving other contacts. READ_SOURCE_TYPE_CONTACT supports basic fields (emailAddresses, metadata, names, phoneNumbers, photos). READ_SOURCE_TYPE_PROFILE supports extended fields (birthdays, genders, organizations, etc.) but requires READ_SOURCE_TYPE_CONTACT to also be included. Applicable only when `other_contacts` is true.; `page_size` (integer) — The number of 'Other Contacts' to return per page. Applicable only when `other_contacts` is true.; `page_token` (string) — An opaque token from a previous response to retrieve the next page of 'Other Contacts' results. Applicable only when `other_contacts` is true and paginating.; `sync_token` (string) — A token from a previous 'Other Contacts' list call to retrieve only changes since the last sync; leave empty for an initial full sync. Applicable only when `other_contacts` is true.; `person_fields` (string) — A comma-separated field mask to restrict which fields on the person (or persons) are returned. Consult the Google People API documentation for a comprehensive list of valid fields. Omitted fields are silently absent from the response — no error is raised. When `other_contacts` is true, only a restricted subset is valid (`emailAddresses`, `names`, `phoneNumbers`, `metadata`); extended fields like `organizations` or `birthdays` may cause validation errors or silent omissions in that mode.; `resource_name` (string) — Resource name identifying the person for whom to retrieve information (like the authenticated user or a specific contact). Used only when `other_contacts` is false. Deleted or stale resource_names may return partial records with missing `emailAddresses`, `names`, or other fields.; `other_contacts` (boolean) — If true, retrieves 'Other Contacts' (people interacted with but not explicitly saved), ignoring `resource_name` and enabling pagination/sync. If false, retrieves information for the single person specified by `resource_name`.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.display_url` (string) — Web UI link for this resource - clickable URL pointing to the relevant Google Contacts page.; `data.people_data` (object) — Contains the person's data as requested, populated when `other_contacts` is false and a single person is fetched.; `data.other_contacts` (object) — Contains the list of 'Other Contacts', potentially including tokens for pagination or future syncs. Populated when `other_contacts` is true.; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/contacts, https://www.googleapis.com/auth/contacts.other.readonly, https://www.googleapis.com/auth/contacts.readonly

##### `GOOGLESUPER_SEARCH_PEOPLE` — COMPLETE

- Description: Searches contacts by matching the query against names, nicknames, emails, phone numbers, and organizations, optionally including 'Other Contacts'. Only searches the authenticated user's contact directory — people existing solely in message headers won't appear; use GMAIL_FETCH_EMAILS for those. Results may be zero or multiple; never auto-select from ambiguous results. Results paginate via next_page_token; follow until empty and deduplicate by email. Many records lack emailAddresses or names even when requested — handle missing keys. Directory/organization policies may suppress entries.
- Required inputs: `query` (string) — Matches contact names, nicknames, email addresses, phone numbers, and organization fields.
- Optional inputs: `page_size` (integer) — Maximum results to return; values >30 are capped to 30 by the API.; `person_fields` (string) — Comma-separated fields to return (e.g., 'names,emailAddresses'). When 'other_contacts' is true, only 'emailAddresses', 'metadata', 'names', 'phoneNumbers' are allowed. For full field access including 'organizations', set 'other_contacts' to false.; `other_contacts` (boolean) — When True, searches both saved contacts and 'Other Contacts' (people you've interacted with but not explicitly saved). Note: This restricts person_fields to only 'emailAddresses', 'metadata', 'names', 'phoneNumbers'. When False, searches only saved contacts but allows all person_fields including 'organizations', 'addresses', etc.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.results` (array) — List of search results, each containing a person record; `data.display_url` (string) — Web UI link for this resource - clickable URL pointing to the Google Contacts web interface; `data.composio_execution_message` (string) — Informational message about any automatic adjustments made during execution; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/contacts, https://www.googleapis.com/auth/contacts.readonly, https://www.googleapis.com/auth/contacts.other.readonly

#### Google Calendar

##### `GOOGLESUPER_EVENTS_LIST` — COMPLETE

- Description: Returns events on the specified calendar. TIMEZONE WARNING: When using timeMin/timeMax with UTC timestamps (ending in 'Z'), the time window is interpreted in UTC regardless of the calendar's timezone. For example, querying '2026-01-19T00:00:00Z' to '2026-01-20T00:00:00Z' on a calendar in America/Los_Angeles (UTC-8) covers 2026-01-18 4pm to 2026-01-19 4pm local time, potentially missing events on the intended local date. To query for a specific local date, use timestamps with the appropriate timezone offset in timeMin/timeMax (e.g., '2026-01-19T00:00:00-08:00' for PST).
- Required inputs: None declared
- Optional inputs: `q` (string) — Free text search terms to find events that match these terms in various fields. Optional.; `iCalUID` (string) — Specifies an event ID in the iCalendar format to be provided in the response. Optional. Use this if you want to search for an event by its iCalendar ID.; `orderBy` (string) — The order of the events returned in the result. Optional. The default is an unspecified, stable order. Acceptable values are: "startTime", "updated". When set to "startTime", singleEvents must be true. The action automatically sets singleEvents=true when orderBy='startTime'.; `timeMax` (string) — Upper bound (exclusive) for an event's start time to filter by. Optional. If unset, no start-time upper bound is applied. Must be an RFC3339 timestamp with mandatory time zone offset (e.g., 2011-06-03T10:00:00-07:00 or 2011-06-03T10:00:00Z). Milliseconds may be provided but are ignored. If timeMin is set, timeMax must be greater than timeMin. TIMEZONE WARNING: If using UTC times (ending in 'Z') but the calendar is in a different timezone, the time window may not align with local calendar dates. For example, '2026-01-19T00:00:00Z' to '2026-01-20T00:00:00Z' covers 2026-01-18 4pm to 2026-01-19 4pm in America/Los_Angeles (UTC-8). To query a specific local date, use timestamps with the appropriate local timezone offset (e.g., '2026-01-19T00:00:00-08:00' for PST). NOTE: Natural language expressions like 'today', 'tomorrow', 'next week' are NOT supported.; `timeMin` (string) — Lower bound (exclusive) for an event's end time to filter by. Optional. If unset, no end-time lower bound is applied. Must be an RFC3339 timestamp with mandatory time zone offset (e.g., 2011-06-03T10:00:00-07:00 or 2011-06-03T10:00:00Z). Milliseconds may be provided but are ignored. If timeMax is set, timeMin must be smaller than timeMax. TIMEZONE WARNING: If using UTC times (ending in 'Z') but the calendar is in a different timezone, the time window may not align with local calendar dates. For example, '2026-01-19T00:00:00Z' to '2026-01-20T00:00:00Z' covers 2026-01-18 4pm to 2026-01-19 4pm in America/Los_Angeles (UTC-8). To query a specific local date, use timestamps with the appropriate local timezone offset (e.g., '2026-01-19T00:00:00-08:00' for PST). NOTE: Natural language expressions like 'today', 'tomorrow', 'next week' are NOT supported.; `timeZone` (string) — Time zone used in the response for formatting event times. Optional. Use an IANA time zone identifier (e.g., America/Los_Angeles). Defaults to the user's primary time zone. Offsets (e.g., '-03:00', 'UTC+0') and abbreviations (e.g., 'IST', 'PST') are invalid. NOTE: This parameter only affects how event times are displayed in the response. It does NOT change how timeMin/timeMax filtering is interpreted. To query a specific local date, use timestamps with the appropriate timezone offset directly in timeMin/timeMax (e.g., '2026-01-19T00:00:00-08:00').; `pageToken` (string) — Opaque pagination token from a previous response's nextPageToken field. Must be the exact string returned by the API - do not use placeholder values like 'NEXT', 'next', '1', '2', etc. Omit this parameter entirely for the first page of results. Optional.; `syncToken` (string) — Token from nextSyncToken to return only entries changed since the last list. Cannot be combined with iCalUID, orderBy, privateExtendedProperty, q, sharedExtendedProperty, timeMin, timeMax, or updatedMin. Deletions since the previous list are always included; showDeleted cannot be false in this mode. The action automatically removes conflicting parameters when syncToken is provided.; `calendarId` (string) — Calendar identifier. Use "primary" for the user's main calendar, or a calendar ID from the user's accessible calendar list. Arbitrary email addresses will NOT work - the calendar must exist in the user's calendar list. Use GOOGLECALENDAR_LIST_CALENDARS to retrieve valid calendar IDs. Defaults to "primary". Empty strings will be treated as "primary". Do NOT use Composio internal IDs like connectedAccountId (which start with "ca_") - these will be automatically replaced with "primary".; `eventTypes` (string) — Event types to return. Optional. Pass a single value only. If unset, returns all event types. Acceptable values are: "birthday", "default", "focusTime", "fromGmail", "outOfOffice", "workingLocation".; `maxResults` (integer) — Maximum number of events returned on one result page. The number of events in the resulting page may be less than this value, or none at all, even if there are more events matching the query. Incomplete pages can be detected by a non-empty nextPageToken field in the response. By default the value is 250 events. The page size can never be larger than 2500 events. Optional. Must be >= 1 if provided.; `updatedMin` (string) — Lower bound for an event's last modification time (RFC3339). When specified, entries deleted since this time are always included regardless of showDeleted. Optional.; … 8 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.etag` (string) — ETag of the collection.; `data.kind` (string) — Type of the collection. Always 'calendar#events'.; `data.items` (array) — List of events on the calendar. Each item is an Event resource.; `data.items[].id` (string) — Opaque identifier of the event.; `data.items[].end` (object) — The (exclusive) end time of the event.; `data.items[].end.date` (string) — The date, in 'yyyy-mm-dd' format, for all-day events.; `data.items[].end.dateTime` (string) — The start/end time as a combined date-time value (RFC3339).; `data.items[].end.timeZone` (string) — The time zone for the start/end time.; `data.items[].etag` (string) — ETag of the resource.; `data.items[].kind` (string) — Type of the resource. Always 'calendar#event' for an event.; `data.items[].start` (object) — The (inclusive) start time of the event.; … 136 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/calendar, https://www.googleapis.com/auth/calendar.app.created, https://www.googleapis.com/auth/calendar.events, https://www.googleapis.com/auth/calendar.events.freebusy, https://www.googleapis.com/auth/calendar.events.owned, https://www.googleapis.com/auth/calendar.events.owned.readonly, https://www.googleapis.com/auth/calendar.events.public.readonly, https://www.googleapis.com/auth/calendar.events.readonly, https://www.googleapis.com/auth/calendar.readonly

##### `GOOGLESUPER_EVENTS_GET` — COMPLETE

- Description: Retrieves a SINGLE event by its unique event_id (REQUIRED). This action does NOT list or search events - it fetches ONE specific event when you already know its ID. If you want to list events within a time range, search for events, or filter by criteria like time_min/time_max, use GOOGLECALENDAR_EVENTS_LIST instead.
- Required inputs: `event_id` (string) — REQUIRED. The unique identifier of the specific event to retrieve. You must already know this ID (e.g., from a previous EVENTS_LIST call or event creation response). This action fetches ONE event by ID - it cannot list or search events. To find events by time range or search criteria, use GOOGLECALENDAR_EVENTS_LIST instead.
- Optional inputs: `time_zone` (string) — Time zone used in the response. If not specified, the calendar's time zone is used.; `calendar_id` (string) — Identifier of the Google Calendar (e.g., email address, specific ID, or 'primary' for the authenticated user's main calendar) from which to retrieve the event.; `max_attendees` (integer) — Maximum number of attendees to include in the response. If there are more than the specified number, only the participant is returned.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (string) — Opaque identifier of the event.; `data.end` (object) — The (exclusive) end time of the event.; `data.end.date` (string) — The date, in 'yyyy-mm-dd' format, for all-day events.; `data.end.dateTime` (string) — The start/end time as a combined date-time value (RFC3339).; `data.end.timeZone` (string) — The time zone for the start/end time.; `data.etag` (string) — ETag of the resource.; `data.kind` (string) — Type of the resource. Always 'calendar#event' for an event.; `data.start` (object) — The (inclusive) start time of the event.; `data.start.date` (string) — The date, in 'yyyy-mm-dd' format, for all-day events.; `data.start.dateTime` (string) — The start/end time as a combined date-time value (RFC3339).; `data.start.timeZone` (string) — The time zone for the start/end time.; … 94 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/calendar, https://www.googleapis.com/auth/calendar.readonly, https://www.googleapis.com/auth/calendar.events, https://www.googleapis.com/auth/calendar.events.readonly, https://www.googleapis.com/auth/calendar.app.created, https://www.googleapis.com/auth/calendar.events.freebusy, https://www.googleapis.com/auth/calendar.events.owned, https://www.googleapis.com/auth/calendar.events.owned.readonly, https://www.googleapis.com/auth/calendar.events.public.readonly

##### `GOOGLESUPER_CREATE_EVENT` — COMPLETE

- Description: Create a Google Calendar event using start_datetime plus duration fields. The organizer is added as an attendee unless exclude_organizer is True. By default adds Google Meet link (works for Workspace, gracefully falls back for personal Gmail). Attendees can be email strings (required) or objects with email and optional fields. No conflict checking is performed; use GOOGLECALENDAR_FREE_BUSY_QUERY to detect overlaps before creating. Returns event id and htmlLink nested under data.response_data. Example: { "start_datetime": "2025-01-16T13:00:00", "timezone": "America/New_York", "event_duration_hour": 1, "event_duration_minutes": 30, "summary": "Client sync", "attendees": ["required@example.com", {"email": "optional@example.com", "optional": true}] }
- Required inputs: `start_datetime` (string) — REQUIRED. Event start time in ISO 8601 format: YYYY-MM-DDTHH:MM:SS. IMPORTANT: Natural language expressions like 'tomorrow', 'next Monday', '2pm tomorrow' are NOT supported and will be rejected. You must provide the exact date and time in ISO format. Fractional seconds (e.g., .000) and timezone info (Z, +, -) will be automatically stripped if provided. Examples: '2025-01-16T13:00:00', '2025-01-16T13:00'.
- Optional inputs: `summary` (string) — Summary (title) of the event.; `location` (string) — Geographic location of the event as free-form text.; `timezone` (string) — IANA timezone name from the timezone database (e.g., 'America/New_York', 'Europe/London', 'Asia/Jerusalem', 'UTC'). Required if datetime is naive. For recurring events, start and end must include a timeZone. If not provided, UTC is used. If datetime includes timezone info (Z or offset), this field is optional and defaults to UTC. IMPORTANT: Must be a valid IANA timezone identifier. Values like 'EST', 'PST', 'ISRAEL TIME', or other abbreviations are NOT valid IANA timezone names.; `attendees` (array) — List of attendees. Each attendee can be either: (1) A string email address (e.g., 'user@example.com'), or (2) An object with 'email' (required), 'optional' (boolean, default false), 'displayName' (string), 'comment' (string), 'additionalGuests' (integer), and 'resource' (boolean). To mark an attendee as optional (not required), use object format: {'email': 'user@example.com', 'optional': true}. IMPORTANT: Only valid email addresses are accepted. Plain names cannot be used.; `eventType` (string) — Type of the event, immutable post-creation. 'workingLocation' (REQUIRES Google Workspace Enterprise). Note: 'fromGmail' events cannot be created via API.; `recurrence` (array) — List of RRULE, EXRULE, RDATE, EXDATE lines for recurring events. Supported frequencies: DAILY, WEEKLY, MONTHLY, YEARLY. For recurring events, start.timeZone and end.timeZone must be present. UNTIL values follow RFC 5545: date-only (YYYYMMDD) for all-day events, or UTC datetime with Z suffix (YYYYMMDDTHHMMSSZ) for timed events. UNTIL values with time but missing Z suffix are auto-corrected. Provide an empty list to remove recurrence so the event becomes non-recurring.; `visibility` (string) — Event visibility: 'default', 'public', 'private', or 'confidential'.; `calendar_id` (string) — Calendar identifier. Use 'primary' (recommended) for the user's main calendar. Alternatively, use a calendar ID from the user's accessible calendar list. Calendar IDs look like email addresses (e.g., 'xyz@group.calendar.google.com' for shared calendars). Important: Arbitrary email addresses will NOT work - the calendar must exist in the user's calendar list with appropriate access permissions. Use GOOGLECALENDAR_LIST_CALENDARS to retrieve valid calendar IDs.; `description` (string) — Description of the event. Can contain HTML. Optional. Must be omitted for 'birthday' event type.; `end_datetime` (string) — Event end time in ISO 8601 format: YYYY-MM-DDTHH:MM:SS. When provided, this parameter takes precedence over event_duration_hour and event_duration_minutes. If not provided, the end time is calculated using start_datetime + duration. Must be after start_datetime. Fractional seconds and timezone info will be automatically stripped if provided. Examples: '2025-01-16T14:30:00', '2025-01-16T14:30'.; `send_updates` (string) — Options for who should receive notifications about event changes.; `transparency` (string) — 'opaque' (busy) or 'transparent' (available).; … 12 more
- Nested inputs: `birthdayProperties.type` (string) — Type of birthday event: 'birthday', 'anniversary', or 'other'. Defaults to 'birthday'.; `birthdayProperties.contact` (string) — Contact ID in format 'people/c12345' from Google People API. REQUIRED when type is 'anniversary' or 'other'. MUST BE OMITTED when type is 'birthday' (the API forbids contact field for type='birthday').; `birthdayProperties.customTypeName` (string) — Custom type name when type is 'other'. Requires valid contact field.; `focusTimeProperties.chatStatus` (string) — Chat status during focus time: 'active' or 'doNotDisturb'.; `focusTimeProperties.declineMessage` (string) — Message to include in declined meeting invitations. Only used when autoDeclineMode is set.; `focusTimeProperties.autoDeclineMode` (string) — Auto decline mode: 'declineNone' (no invitations declined), 'declineAllConflictingInvitations' (all conflicting invitations declined), or 'declineOnlyNewConflictingInvitations' (only new conflicting invitations declined).; `outOfOfficeProperties.declineMessage` (string) — Message to include in declined meeting invitations. Only used when autoDeclineMode is set.; `outOfOfficeProperties.autoDeclineMode` (string) — Auto decline mode: 'declineNone' (no invitations declined), 'declineAllConflictingInvitations' (all conflicting invitations declined), or 'declineOnlyNewConflictingInvitations' (only new conflicting invitations declined). RECURRING EVENT RESTRICTION: For recurring out-of-office events, ONLY 'declineOnlyNewConflictingInvitations' is allowed. Cannot use 'declineAllConflictingInvitations' for recurring OOO (Google Calendar prevents retroactively declining existing meetings). REQUIRES Google Workspace (paid business subscription, e.g., Business Starter at $6/user/month). Personal Gmail accounts cannot use this feature.; `workingLocationProperties.type` (string) — Type of working location ('homeOffice' | 'officeLocation' | 'customLocation').; `workingLocationProperties.homeOffice` (object) — Empty object marker for home office working location.

This is used to indicate the user is working from home.
Google Calendar API accepts an empty object for this field.; `workingLocationProperties.customLocation` (object) — Custom working location with a display label.; `workingLocationProperties.customLocation.label` (string) — Label for a custom working location (e.g., 'Client site').; … 6 more
- Output fields: `data` (object) — Data from the action execution; `data.display_url` (string) — URL to view the event in Google Calendar web UI. Extracted from the API response htmlLink field.; `data.response_data` (object) — Created Google Calendar event resource (calendar#event).; `data.response_data.id` (string) — Opaque identifier of the event.; `data.response_data.end` (object) — The (exclusive) end time of the event.; `data.response_data.end.date` (string) — The date, in 'yyyy-mm-dd' format, for all-day events.; `data.response_data.end.dateTime` (string) — The start/end time as a combined date-time value (RFC3339).; `data.response_data.end.timeZone` (string) — The time zone for the start/end time.; `data.response_data.etag` (string) — ETag of the resource.; `data.response_data.kind` (string) — Type of the resource. Always 'calendar#event' for an event.; `data.response_data.start` (object) — The (inclusive) start time of the event.; `data.response_data.start.date` (string) — The date, in 'yyyy-mm-dd' format, for all-day events.; … 125 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/calendar, https://www.googleapis.com/auth/calendar.app.created, https://www.googleapis.com/auth/calendar.events, https://www.googleapis.com/auth/calendar.events.owned, https://www.googleapis.com/auth/calendar.calendars, https://www.googleapis.com/auth/calendar.calendars.readonly, https://www.googleapis.com/auth/calendar.readonly

#### Google Drive

##### `GOOGLESUPER_LIST_FILES` — COMPLETE

- Description: DEPRECATED: Use GOOGLEDRIVE_FIND_FILE instead. Tool to list a user's files and folders in Google Drive. Use this to search or browse for files and folders based on various criteria.
- Required inputs: None declared
- Optional inputs: `q` (string) — A query string for filtering the file results. Supports operators 'and', 'or', 'not'. VALID query terms: 'name' (contains, =, !=), 'fullText' (contains), 'mimeType' (contains, =, !=), 'modifiedTime' (<=, <, =, !=, >, >=), 'viewedByMeTime' (<=, <, =, !=, >, >=), 'trashed' (=, !=), 'starred' (=, !=), 'parents' (in), 'owners' (in), 'writers' (in), 'readers' (in), 'sharedWithMe' (=, !=), 'createdTime' (<=, <, =, !=, >, >=), 'properties' (has), 'appProperties' (has), 'visibility' (=, !=), 'shortcutDetails.targetId' (=, !=). IMPORTANT: 'id' is NOT a valid query term - you cannot search by file ID using this parameter. To get a specific file by ID, use the 'Get File Metadata' action instead. Example: "name contains 'important' and mimeType = 'application/vnd.google-apps.folder'".; `fields` (string) — Selector specifying which file fields to include in the response. Provide a comma-separated list of file field names (e.g., 'id,name,mimeType,webViewLink'). The action will automatically format this into the proper API format 'files(field1,field2,...)'. Common file fields include: id, name, description, mimeType, webViewLink, webContentLink, size, createdTime, modifiedTime, parents, owners, permissions. To also include the pagination token, add 'nextPageToken' to the list. NOTE: Google Drive API v2 field names are automatically converted to v3 equivalents (e.g., alternateLink→webViewLink, downloadUrl→webContentLink, title→name, createdDate→createdTime, modifiedDate→modifiedTime).; `spaces` (string) — A comma-separated list of spaces to query within the corpora. Supported values are 'drive' and 'appDataFolder'. 'drive' represents files in My Drive and shared drives, while 'appDataFolder' represents the application's private data folder.; `corpora` (string) — Specifies the bodies of items (files/documents) to which the query applies. Supported values are 'user', 'domain', 'drive', and 'allDrives'. It's generally more efficient to use 'user' or 'drive' instead of 'allDrives'. Defaults to 'user'.; `driveId` (string) — The ID of the shared drive to search. This is used when `corpora` is set to 'drive'.; `orderBy` (string) — A comma-separated list of sort keys. Valid keys are: 'createdTime', 'folder', 'modifiedByMeTime', 'modifiedTime', 'name', 'name_natural', 'quotaBytesUsed', 'recency', 'sharedWithMeTime', 'starred', 'viewedByMeTime'. IMPORTANT: Use 'quotaBytesUsed' to sort by file size (do NOT use 'size' - it is not a valid key). Each key sorts in ascending order by default, but can be reversed with the 'desc' modifier (e.g., 'modifiedTime desc').; `folderId` (string) — ID of a specific folder to list files from. This is a convenience parameter that automatically adds "'folder_id' in parents" to the query. Cannot be used together with a custom 'q' parameter.; `pageSize` (integer) — The maximum number of files to return per page. The value must be between 1 and 1000, inclusive. Defaults to 100.; `pageToken` (string) — The token for continuing a previous list request on the next page. This MUST be set to the value of 'nextPageToken' from the previous response. Do not manually construct or modify pageToken values as they are opaque tokens generated by the API. If the token is rejected, pagination should be restarted from the first page.; `includeLabels` (string) — A comma-separated list of label IDs to include in the `labelInfo` part of the response for each file.; `supportsAllDrives` (boolean) — Whether the requesting application supports both My Drives and shared drives. Defaults to false. If true, then `includeItemsFromAllDrives` can be used to extend the search to all drives.; `includeItemsFromAllDrives` (boolean) — Whether to include items from both My Drive and shared drives. This is relevant when `corpora` is 'user' or 'domain'. Defaults to false.; … 1 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.kind` (string) — Identifies the resource type of the response. For files.list this is always 'drive#fileList'.; `data.files` (array) — The list of files. Each item is a File resource. The fields included for each file depend on the 'fields' query parameter. May be empty if no items match.; `data.nextPageToken` (string) — Token for continuing a truncated list request. Use this value in the 'pageToken' parameter of a subsequent request to retrieve the next page of results. Present only if there are more files to retrieve.; `data.incompleteSearch` (boolean) — Whether the search process was incomplete. If true, then some search results may be missing.; `data.composio_execution_message` (string) — Message about input modifications made during execution.; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: yes; scopes: https://www.googleapis.com/auth/drive, https://www.googleapis.com/auth/drive.appdata, https://www.googleapis.com/auth/drive.file, https://www.googleapis.com/auth/drive.meet.readonly, https://www.googleapis.com/auth/drive.metadata, https://www.googleapis.com/auth/drive.metadata.readonly, https://www.googleapis.com/auth/drive.photos.readonly, https://www.googleapis.com/auth/drive.readonly

##### `GOOGLESUPER_GET_FILE_METADATA` — PARTIAL

- Description: Tool to get a file's metadata by ID. Use to verify `mimeType`, `parents`, and `trashed` status before destructive operations (delete/move/export), or to confirm `mimeType='application/vnd.google-apps.document'` before calling GOOGLEDOCS_* tools (non-native files require GOOGLEDRIVE_DOWNLOAD_FILE). Only returns metadata visible to the connected account; public access requires GOOGLEDRIVE_ADD_FILE_SHARING_PREFERENCE. High-frequency calls risk `403 rateLimitExceeded`; apply exponential backoff.
- Required inputs: `fileId` (string) — The Google Drive file ID (an opaque alphanumeric string like '1a2b3c4d5e6f7g8h9i0j'), NOT a file name. If you only have a file name, use GOOGLEDRIVE_FIND_FILE or GOOGLEDRIVE_LIST_FILES to get the file ID first.
- Optional inputs: `fields` (string) — Comma-separated list of fields to include in the response. Use this for partial responses to request only specific metadata fields. Common fields: id, name, mimeType, webViewLink, webContentLink, createdTime, modifiedTime, size, quotaBytesUsed, parents, owners, permissions. Use '*' to return all available fields. Note: The deprecated v2 field 'alternateLink' is automatically migrated to 'webViewLink'. Example: 'id,name,mimeType,webViewLink,createdTime,modifiedTime'. Most fields (webViewLink, parents, owners, size, modifiedTime, etc.) are omitted by default — explicitly list required fields or use '*' (increases latency). `md5Checksum` is null for native Google Workspace files (Docs/Sheets/Slides); use `mimeType` to classify items — folders use `mimeType='application/vnd.google-apps.folder'` and Workspace files return `size=null`. `modifiedTime` is RFC 3339 UTC format.; `includeLabels` (string) — A comma-separated list of IDs of labels to include in the labelInfo part of the response.; `supportsAllDrives` (boolean) — Whether the requesting application supports both My Drives and shared drives. Defaults to true to ensure files in shared drives are accessible.; `includePermissionsForView` (string) — Specifies which additional view's permissions to include in the response. Only 'published' is supported.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (string) — The ID of the file.; `data.kind` (string) — Identifies this resource as a Drive file. Always "drive#file" when present.; `data.name` (string) — The name of the file (including extension when applicable).; `data.size` (string); `data.owners` (array); `data.owners[].me` (boolean); `data.owners[].kind` (string) — Identifies this resource as a user. Always 'drive#user' when present.; `data.owners[].photoLink` (string); `data.owners[].displayName` (string); `data.owners[].emailAddress` (string); `data.owners[].permissionId` (string); … 172 more
- Output assessment: Top-level output is a generic wrapper around nested data. Many output fields lack descriptions.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/drive, https://www.googleapis.com/auth/drive.appdata, https://www.googleapis.com/auth/drive.file, https://www.googleapis.com/auth/drive.meet.readonly, https://www.googleapis.com/auth/drive.metadata, https://www.googleapis.com/auth/drive.metadata.readonly, https://www.googleapis.com/auth/drive.photos.readonly, https://www.googleapis.com/auth/drive.readonly

##### `GOOGLESUPER_FIND_FILE` — COMPLETE

- Description: The comprehensive Google Drive search tool that handles all file and folder discovery needs. Use this for any file finding task - from simple name searches to complex queries with date filters, MIME types, permissions, custom properties, folder scoping, and more. Searches across My Drive and shared drives with full metadata support. Examples: - Find PDFs: q="mimeType = 'application/pdf'" - Find recent files: q="modifiedTime > '2024-01-01T00:00:00'" - Search by name: q="name contains 'report'" - Files in folder: folderId="abc123" or q="'FOLDER_ID' in parents"
- Required inputs: None declared
- Optional inputs: `q` (string) — Query string to filter file results. Accepts both simple text searches and full Google Drive query syntax.

        **Simple Text Search:** Bare text (e.g., "SAM RFP") is auto-converted to fullText search. Bare email addresses are auto-converted to owner search.

        **Full Query Syntax:** 'field operator value' combined with 'and', 'or', 'not'

        **Operators:** =, !=, <, >, <=, >=, contains, in

        **Common Fields:**
        - `name` - File name (exact match with = or partial match with contains)
        - `fullText` - File content search
        - `mimeType` - File type (e.g., 'application/pdf', 'application/vnd.google-apps.folder')
        - `modifiedTime`, `createdTime` - Dates (RFC 3339: '2024-01-01T00:00:00')
        - `parents` - Folder IDs containing the file
        - `owners`, `writers` - User email addresses (MUST use 'in' operator, NOT colon syntax)
        - `properties`, `appProperties` - Custom metadata

        **Boolean Filter Fields (sharedWithMe, trashed, starred):**
        These fields require explicit `= true` or `= false` syntax:
        - `sharedWithMe = true` - Find files shared with you by others
        - `sharedWithMe = false` - Find files NOT shared with you (your own files)
        - `trashed = true` - Find files in trash
        - `trashed = false` - Exclude trashed files from results
        - `starred = true` - Find starred/favorited files
        - `starred = false` - Find non-starred files

        Combine with other conditions using 'and':
        - "sharedWithMe = true and name contains 'report'" - Find shared files with 'report' in name
        - "sharedWithMe = true and mimeType = 'application/pdf'" - Find shared PDF files
        - "starred = true and modifiedTime > '2024-01-01T00:00:00'" - Find recently modified starred files

        **Query Complexity Limits:**
        Queries with many OR clauses (typically >5-10) may fail with 'The query is too complex' error.

        **Name Field Usage:**
        Wildcards (*) are NOT supported. Use 'contains' operator for partial matching.

        **User Email Searches:**
        - CORRECT: "'user@example.com' in owners" or "'user@example.com' in writers" or "'user@example.com' in readers"
        - INCORRECT: "owner:user@example.com" (colon syntax is NOT supported and will cause errors)
        - Always use the 'in' operator with quoted email addresses for user-based searches

        **Special Syntax:**
        - Dates: RFC 3339 format (time zone defaults to UTC)
        - Apostrophes/quotes in values: Automatically escaped. You can write "name = 'Jan'26'" or "name = 'Valentine's Day'" without manual escaping.
        - Grouping: Use parentheses for OR: "(mimeType contains 'image/' or mimeType contains 'video/')"
        - Custom properties: "properties has { key='department' and value='sales' }"

        **IMPORTANT - Root Folder ('My Drive'):**
        'My Drive' is NOT a searchable folder name. To work with the root folder, use the 'root' alias: folder_id='root' or "'root' in parents" in your query.
        ; `fields` (string) — Selector specifying which fields to include in a partial response. Use '*' for all fields.

**Default Behavior (Recommended for Discovery):**
When omitted, returns essential file discovery fields: id, name, mimeType, size, modifiedTime, createdTime, parents, webViewLink, trashed, starred. This lightweight default is optimized for file search/discovery use cases without verbose permission or capability metadata.

**Format:** For file fields, use 'files(field1,field2,...)' format. For example: 'files(id,name,mimeType)'.
Top-level response fields (kind, nextPageToken, incompleteSearch) can be used directly.

**Note:** Bare field names like 'id,name,mimeType' will be automatically wrapped in 'files()' for convenience.
The 'editors' field is not valid in Drive API v3; use 'permissions' instead for access control information.; `spaces` (string) — A comma-separated list of spaces to query. Supported values are 'drive', 'appDataFolder' and 'photos'.; `corpora` (string) — Specifies which collections of files to search. Defaults to 'allDrives' (searches My Drive + all accessible shared drives).

        **Values:**
        - `user` - Search only user's personal My Drive
        - `domain` - Search all files shared within Google Workspace domain
        - `drive` - Search specific shared drive (requires 'driveId' parameter and 'includeItemsFromAllDrives' must be true)
        - `allDrives` - Search My Drive + all accessible shared drives (DEFAULT, requires 'includeItemsFromAllDrives' to be true)

        **When to Use:**
        - Personal files only: Use 'user'
        - Organization-wide: Use 'domain'
        - Specific shared drive: Use 'drive' with 'driveId'
        - Maximum coverage: Use 'allDrives' (auto-enables supportsAllDrives and includeItemsFromAllDrives)
        ; `driveId` (string) — ID of the shared drive to search. When provided, 'corpora' will automatically be set to 'drive' (mutually exclusive with corpora='allDrives'). Required if 'corpora' is 'drive'.; `orderBy` (string) — Comma-separated sort keys. Ascending by default; add 'desc' for descending. Cannot be used when query (q) contains fullText search terms.

        **Valid Keys:**
        - `createdTime`, `modifiedTime`, `modifiedByMeTime` - Dates
        - `viewedByMeTime`, `sharedWithMeTime` - Activity dates
        - `name`, `name_natural` - File name (natural: file1, file2, file10)
        - `folder` - Folder hierarchy
        - `quotaBytesUsed` - Storage size (NOTE: 'size' is NOT valid, use 'quotaBytesUsed')
        - `starred` - Starred status
        - `recency` - Recent activity (combines view time and modification time for relevance-based sorting)

        **Important:** 'size' is NOT a valid sort key. Use 'quotaBytesUsed' to sort by file size.

        **Restriction:** Sorting is not supported when the query contains fullText searches (e.g., "fullText contains 'keyword'"). Omit orderBy when using fullText queries.
        ; `pageSize` (integer) — The maximum number of files to return per page.; `folder_id` (string) — ID of a specific folder to search within. This automatically adds "'folder_id' in parents" to the query. Can be combined with the 'q' parameter to further filter results within the folder. Use 'root' to search within the user's root folder (My Drive). Note: 'My Drive' is not a searchable folder name - use 'root' alias instead.; `pageToken` (string) — The token for continuing a previous list request on the next page. IMPORTANT: This must be the exact opaque string from a previous response's 'nextPageToken' field - do not modify, truncate, URL-encode, or construct tokens manually. Invalid or corrupted tokens will result in API errors.; `include_labels` (string) — A comma-separated list of label IDs to include in the `labelInfo` part of the response for each file. Empty strings are automatically treated as omitted.; `pagetoken_dropped` (boolean) — Indicates whether the page token was dropped from the request.; `supportsAllDrives` (boolean) — Whether the requesting application supports both My Drives and shared drives. If 'includeItemsFromAllDrives' is true, this must also be true.; … 10 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.kind` (string) — Identifies what kind of resource this is. Value: the fixed string \"drive#fileList\".; `data.files` (array) — The list of files. If nextPageToken is populated, then this list may be incomplete and an additional page of results should be fetched.; `data.files[].id` (string) — The ID of the file.; `data.files[].kind` (string) — Identifies what kind of resource this is. Value: the fixed string "drive#file".; `data.files[].name` (string) — The name of the file.; `data.files[].size` (string) — The size of the file's content in bytes (int64 as string). Only applicable to files with content stored in Drive.; `data.files[].owners` (array) — The owners of the file. Each owner is a user.; `data.files[].owners[].me` (boolean) — Whether this user is the requesting user.; `data.files[].owners[].kind` (string) — Identifies what kind of resource this is.; `data.files[].owners[].photoLink` (string) — A link to the user's profile photo, if available.; `data.files[].owners[].displayName` (string) — The display name of the user.; … 178 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260714_00; available versions: 20260714_00
- Deprecated: no; scopes: https://www.googleapis.com/auth/drive, https://www.googleapis.com/auth/drive.appdata, https://www.googleapis.com/auth/drive.file, https://www.googleapis.com/auth/drive.meet.readonly, https://www.googleapis.com/auth/drive.metadata, https://www.googleapis.com/auth/drive.metadata.readonly, https://www.googleapis.com/auth/drive.photos.readonly, https://www.googleapis.com/auth/drive.readonly

### github

#### GitHub repositories

##### `GITHUB_FIND_REPOSITORIES` — COMPLETE

- Description: AI-optimized repository search with smart filtering by language, stars, topics, and ownership. Builds intelligent search queries and returns clean, actionable repository data. Check `incomplete_results` in the response — when true, results are non-exhaustive. Search endpoints are rate-limited to ~30 requests/minute (vs. 5000/hour for general REST); apply backoff on 403/429. Total results are capped at ~1000 per query regardless of pagination.
- Required inputs: `query` (string) — Search query for repository name, description, or README content. Accepts both 'query' and 'q' as parameter names. Supports OR operator for alternatives (e.g., 'bug OR error'). Multi-word phrases in OR expressions are automatically quoted. GitHub limits OR expressions to 6 terms maximum; queries exceeding this limit are truncated. If using GitHub qualifiers like 'repo:', 'user:', or 'org:', they must be properly formatted (e.g., 'repo:owner/name', 'user:username', 'org:orgname'). Note: 'owner:' is not a valid GitHub qualifier; if used, it will be automatically transformed to 'user:'. Must be non-empty; blank strings trigger a 'Search query cannot be empty' validation error.
- Optional inputs: `page` (integer) — Page number of results to fetch.; `sort` (string) — Field to sort results by.; `order` (string) — Sort order (ascending or descending).; `owner` (string) — Filter by specific user or organization. Do NOT use this parameter if your 'query' already contains 'user:', 'org:', or 'repo:' qualifiers - they will conflict. Use either 'owner' parameter OR search qualifiers in 'query', not both.; `topic` (string) — Filter by repository topic/tag.; `archived` (boolean) — Filter by archived status. None means include both.; `language` (string) — Filter by programming language.; `per_page` (integer) — Number of results per page (maximum 100). GitHub caps total results at ~1000 per query; for exhaustive searches, split by `language`, `topic`, `min_stars`/`max_stars`, or `owner`.; `max_stars` (integer) — Maximum number of stars the repository should have.; `min_forks` (integer) — Minimum number of forks the repository should have.; `min_stars` (integer) — Minimum number of stars the repository should have.; `fork_filter` (string) — How to handle forks: 'include' (include forks), 'exclude' (exclude forks), 'only' (only forks).; … 2 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.items` (array) — Array of repository objects matching the search criteria; `data.items[].id` (integer) — Repository ID; `data.items[].url` (string) — API URL for the repository; `data.items[].fork` (boolean) — Whether repository is a fork; `data.items[].name` (string) — Repository name; `data.items[].size` (integer) — Size in kilobytes, calculated hourly; `data.items[].forks` (integer) — Number of forks; `data.items[].owner` (object) — Repository owner information; `data.items[].owner.id` (integer) — User ID; `data.items[].owner.url` (string) — API URL; `data.items[].owner.type` (string) — Account type (User/Organization); … 66 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo, public_repo

##### `GITHUB_CREATE_REPOSITORY` — COMPLETE

- Description: Creates a new repository for the authenticated user. Use this action when you need to create a new GitHub repository under the authenticated user's account. The repository name must be unique for the user, and various settings can be configured such as visibility (public/private), enabled features (issues, wiki, projects), and merge options.
- Required inputs: `name` (string) — The name of the repository.
- Optional inputs: `private` (boolean) — Whether the repository is private. Default is false (public).; `has_wiki` (boolean) — Whether the wiki is enabled. Default is true.; `homepage` (string) — A URL with more information about the repository.; `auto_init` (boolean) — Whether the repository is initialized with a minimal README. Default is false.; `has_issues` (boolean) — Whether issues are enabled. Default is true.; `description` (string) — A short description of the repository.; `is_template` (boolean) — Whether this repository acts as a template that can be used to generate new repositories. Default is false.; `has_projects` (boolean) — Whether projects are enabled. Default is true.; `has_downloads` (boolean) — Whether downloads are enabled. Default is true.; `has_discussions` (boolean) — Whether discussions are enabled. Default is false.; `allow_auto_merge` (boolean) — Whether to allow Auto-merge to be used on pull requests. Default is false.; `license_template` (string) — The license keyword of the open source license for this repository.; … 9 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — The unique identifier of the repository.; `data.url` (string) — The API URL for the repository.; `data.fork` (boolean) — Whether the repository is a fork.; `data.name` (string) — The name of the repository.; `data.size` (integer) — The size of the repository in kilobytes.; `data.forks` (integer) — The number of forks.; `data.owner` (object) — The owner of the repository.; `data.owner.id` (integer) — The unique numerical identifier for the user.; `data.owner.url` (string) — The API URL for this user's details.; `data.owner.name` (string) — The display name of the user.; `data.owner.type` (string) — The type of GitHub account (e.g., 'User', 'Organization').; … 148 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: public_repo, repo

##### `GITHUB_CREATE_AN_ORGANIZATION_REPOSITORY` — COMPLETE

- Description: Creates a new repository within a specified GitHub organization, with options for detailed configuration including visibility, features, merge strategies, initial commit, and templates.
- Required inputs: `org` (string) — Name of the organization where the repository will be created (case-insensitive).; `name` (string) — Name of the new repository.
- Optional inputs: `private` (boolean) — Whether the repository is private. `visibility` takes precedence if both are set.; `team_id` (integer) — ID of the team to grant access to this repository within the organization.; `has_wiki` (boolean) — Enable wiki for this repository.; `homepage` (string) — URL for the repository's homepage.; `auto_init` (boolean) — Create an initial commit with an empty README.; `has_issues` (boolean) — Enable issues for this repository.; `visibility` (string) — Repository visibility: 'public' (visible to everyone) or 'private' (visible to collaborators).; `description` (string) — Short description of the repository.; `is_template` (boolean) — Make this repository a template repository.; `has_projects` (boolean) — Enable projects for this repository. Fails if organization has disabled repository projects and this is true.; `has_downloads` (boolean) — Enable downloads for this repository (deprecated).; `allow_auto_merge` (boolean) — Allow auto-merge on pull requests.; … 12 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique identifier for the repository.; `data.url` (string) — API URL for the repository.; `data.fork` (boolean) — Whether this repository is a fork.; `data.name` (string) — The name of the repository.; `data.size` (integer) — Size of the repository in kilobytes.; `data.forks` (integer) — Number of forks (same as forks_count).; `data.owner` (object) — Information about the repository owner (user or organization).; `data.owner.id` (integer) — Unique identifier for the owner.; `data.owner.url` (string) — API URL for the owner.; `data.owner.type` (string) — Type of owner account (e.g., 'User', 'Organization').; `data.owner.login` (string) — The username of the owner.; … 121 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub issues

##### `GITHUB_GET_AN_ISSUE` — COMPLETE

- Description: Retrieves detailed information about a specific issue in a repository using the owner, repository name, and issue number.
- Required inputs: `repo` (string) — Name of the repository (without `.git` extension). Not case-sensitive.; `owner` (string) — Username of the account owning the repository. Not case-sensitive.; `issue_number` (integer) — The identifying number of the issue.
- Optional inputs: None declared
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique identifier of the issue.; `data.url` (string) — API URL for the issue.; `data.body` (string) — Contents of the issue.; `data.type` (object) — Issue Type object representing the type of issue.; `data.type.id` (integer) — Unique identifier of the issue type.; `data.type.name` (string) — Name of the issue type.; `data.type.color` (string) — Color code of the issue type.; `data.type.node_id` (string) — GraphQL node identifier of the issue type.; `data.type.created_at` (string) — Timestamp when the issue type was created.; `data.type.is_enabled` (boolean) — Whether the issue type is enabled.; `data.type.updated_at` (string) — Timestamp when the issue type was last updated.; … 341 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_CREATE_AN_ISSUE` — COMPLETE

- Description: Creates a new issue in a GitHub repository, requiring the repository to exist and have issues enabled; specific fields like assignees, milestone, or labels may require push access.
- Required inputs: `repo` (string) — The name of the repository, without the `.git` extension (case-insensitive). The repository must exist, be accessible, and have issues enabled.; `owner` (string) — The GitHub account owner of the repository (case-insensitive). The repository must exist and be accessible to the authenticated user.; `title` (string) — The title for the new issue.
- Optional inputs: `body` (string) — The detailed textual contents of the new issue.; `labels` (array) — Array of label names to associate with this issue (generally case-insensitive). NOTE: Only users with push access can set labels; they are silently dropped otherwise. Pass an empty list to clear all labels.; `assignee` (string) — Login for the user to whom this issue should be assigned. NOTE: Only users with push access can set the assignee; it is silently dropped otherwise. **This field is deprecated in favor of `assignees`.**; `assignees` (array) — GitHub login names for users to assign to this issue. NOTE: Only users with push access can set assignees; they are silently dropped otherwise.; `milestone` (string) — The ID of the milestone to associate this issue with (e.g., "5"). NOTE: Only users with push access can set the milestone; it is silently dropped otherwise.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique identifier for the issue.; `data.url` (string) — API URL for the issue.; `data.body` (string) — Body content of the issue in markdown format. Returns null if no body content.; `data.type` (string) — Issue type name if issue types are enabled for the repository.; `data.user` (object) — User who created the issue.; `data.user.id` (integer) — User ID.; `data.user.url` (string) — API URL for the user.; `data.user.type` (string) — Type of user account (e.g., 'User', 'Organization').; `data.user.login` (string) — Username of the user.; `data.user.node_id` (string) — Global node ID for the user.; `data.user.html_url` (string) — GitHub profile URL for the user.; … 295 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: public_repo, repo

##### `GITHUB_LIST_REPOSITORY_ISSUES` — COMPLETE

- Description: Lists issues (including pull requests) from a GitHub repository. Requires BOTH 'owner' AND 'repo' parameters (e.g., owner='octocat', repo='Hello-World' for github.com/octocat/Hello-World).
- Required inputs: `repo` (string) — REQUIRED. The name of the repository (without .git extension). For 'octocat/Hello-World', repo is 'Hello-World'. Not case-sensitive. Must be provided together with 'owner'.; `owner` (string) — REQUIRED. The GitHub account owner (user or organization) of the repository. For 'octocat/Hello-World', owner is 'octocat'. Not case-sensitive. Must be provided together with 'repo'.
- Optional inputs: `page` (integer) — Page number for the set of results. Refer to GitHub's REST API documentation for pagination details.; `sort` (string) — The field to sort the results by.; `type` (string) — Filter issues by type name. Use '*' for issues with any type, 'none' for issues without a type, or specify a type name.; `since` (string) — Filters issues to include only those updated at or after the specified time. This should be a timestamp in ISO 8601 format (e.g., `YYYY-MM-DDTHH:MM:SSZ`).; `state` (string) — Filters issues based on their state.; `labels` (string) — A comma-separated list of label names to filter issues by. Example: 'bug,ui,@high'.; `creator` (string) — Filter issues by the username of the user who created the issue.; `assignee` (string) — Filter issues by assignee's username. Use 'none' for issues with no assignee, or '*' for issues assigned to any user.; `per_page` (integer) — Number of results per page (max 100). Default is 30, which matches GitHub's API default. Refer to GitHub's REST API documentation for pagination details.; `direction` (string) — The direction of the sort.; `mentioned` (string) — Filter issues by a user mentioned in the issue's description or comments. Provide the username.; `milestone` (string) — Filter issues by milestone. Provide the milestone `number` as a string, '*' for issues with any milestone, or 'none' for issues without a milestone.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.issues` (array) — Array of issue objects from the repository.; `data.issues[].id` (integer) — Unique identifier for the issue.; `data.issues[].url` (string) — API URL for this issue.; `data.issues[].body` (string) — Body text content of the issue (markdown format).; `data.issues[].user` (object) — User who created the issue.; `data.issues[].user.id` (integer) — Unique identifier for the user.; `data.issues[].user.url` (string) — API URL for the user.; `data.issues[].user.type` (string) — Type of account (User, Organization, Bot, etc.).; `data.issues[].user.login` (string) — Username of the user.; `data.issues[].user.node_id` (string) — Global node ID for the user.; `data.issues[].user.html_url` (string) — GitHub web URL for the user's profile.; … 178 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub pull requests

##### `GITHUB_FIND_PULL_REQUESTS` — COMPLETE

- Description: Primary tool to find and search pull requests. Supports filtering by repository, author, state, labels, and merge status, and returns structured PR data for reliable use in workflows. GitHub search results are capped at ~1000 total items; narrow filters when totals approach this limit. `created_since`/`updated_since` filter on creation/update timestamps, not `merged_at`; apply merge-date filtering client-side.
- Required inputs: None declared
- Optional inputs: `page` (integer) — Page number of results to fetch.; `repo` (string) — Filter by specific repository. Can be provided in 'owner/repo' format (e.g., 'facebook/react'), or as just the repo name when used with the owner parameter. When owner and repo are both provided and have the same value, they are combined as 'owner/repo'. When omitted along with owner, search scope is determined by for_authenticated_user flag. The repository must exist and be accessible with your token. Prefer 'owner/repo' format; bare repo name without the `owner` parameter causes invalid repository format errors.; `sort` (string) — Field to sort results by. Set explicitly to 'updated' with `order=desc` when recency matters; default behavior may return best-match relevance ordering instead.; `label` (string) — Filter by label name.; `order` (string) — Sort order (ascending or descending).; `owner` (string) — Filter by repository owner (user or organization). Can be used alone to search all repositories for an owner, or combined with repo parameter to specify owner and repo separately. When both owner and repo are provided, the search is scoped to 'owner/repo'. When omitted along with repo, search scope is determined by for_authenticated_user flag. The owner must exist and repositories must be accessible with your token.; `query` (string) — Optional search query for PR title, description, or commit messages. Supports GitHub search syntax including label filters (e.g., 'label:bug'), logical operators (AND, OR, NOT), and text search. If not provided, PRs will be listed based on other filter parameters. Type qualifiers (type:pr, is:pr, is:issue, type:issue) are automatically normalized. When using logical operators with qualifiers like labels, the query is automatically grouped for proper parsing. When omitting query, ensure at least one structured filter parameter is provided; passing no query and no filters may cause validation errors. Prefer dedicated parameters (`label`, `state`, `author`) over embedding qualifiers in the query string. Exact title matching is not supported; post-filter on title client-side if needed.; `state` (string) — Filter by PR state: 'open', 'closed', or 'all'.; `author` (string) — Filter by PR author username. Must be a valid GitHub username (alphanumeric characters and hyphens only, cannot start or end with hyphen). No dots or other special characters allowed.; `assignee` (string) — Filter by PR assignee username. Use '@me' for current user. Must be a valid GitHub username (alphanumeric characters and hyphens only, cannot start or end with hyphen). No dots or other special characters allowed.; `language` (string) — Filter by programming language.; `mentions` (string) — Filter by username mentioned in PR. Must be a valid GitHub username (alphanumeric characters and hyphens only, cannot start or end with hyphen). No dots or other special characters allowed.; … 8 more
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.total_count` (integer) — Total number of pull requests matching the search query. Useful for understanding result set size and pagination.; `data.pull_requests` (array) — Array of pull request objects; `data.pull_requests[].id` (integer) — Unique identifier for the pull request across all of GitHub.; `data.pull_requests[].url` (string) — API URL for this pull request.; `data.pull_requests[].base` (object) — Target branch information (where changes will be merged).; `data.pull_requests[].base.ref` (string) — Branch name.; `data.pull_requests[].base.sha` (string) — Commit SHA at the head of the branch.; `data.pull_requests[].base.repo` (unknown) — Full repository object.; `data.pull_requests[].base.user` (unknown) — Owner of the repository.; `data.pull_requests[].base.label` (string) — Label in 'user:branch' format.; `data.pull_requests[].body` (string) — Description or body content of the pull request in markdown format.; … 121 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: public_repo, repo

##### `GITHUB_GET_A_PULL_REQUEST` — COMPLETE

- Description: Retrieves a specific pull request from a GitHub repository using its owner, repository name, and pull request number.
- Required inputs: `repo` (string) — The name of the repository, without the .git extension. This value is not case sensitive.; `owner` (string) — The username of the account that owns the repository. This value is not case sensitive.; `pull_number` (integer) — The number that identifies the specific pull request.
- Optional inputs: None declared
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique identifier for the pull request.; `data.url` (string) — API URL for the pull request.; `data.base` (object) — The base branch information (target branch).; `data.base.ref` (string) — Branch name.; `data.base.sha` (string) — Commit SHA.; `data.base.repo` (object) — Repository object with full repository details (null if repository has been deleted).; `data.base.repo.id` (integer) — Repository ID.; `data.base.repo.url` (string) — API URL for the repository.; `data.base.repo.fork` (boolean) — Whether this is a fork.; `data.base.repo.name` (string) — Repository name.; `data.base.repo.size` (integer) — Repository size in kilobytes.; … 342 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_CREATE_A_PULL_REQUEST` — COMPLETE

- Description: Creates a pull request in a GitHub repository, requiring existing `base` and `head` branches; `title` or `issue` must be provided.
- Required inputs: `base` (string) — The name of the branch you want the changes pulled into. This must be an existing branch on the current (target) repository. You cannot submit a pull request to one repository that requests a merge to a base branch of another repository. ; `head` (string) — The name of the branch where your changes are implemented. IMPORTANT: This branch must already exist in the repository (or fork) before calling this action. You must first create the branch and push commits to it (using git or the 'Create a reference' action), then create the PR. For cross-repository pull requests, namespace `head` with the source owner and branch, like `username:branch`.; `repo` (string) — The name of the repository, without the `.git` extension. This name is not case-sensitive.; `owner` (string) — The account owner of the repository. This name is not case-sensitive.
- Optional inputs: `body` (string) — The detailed description or contents of the pull request.; `draft` (boolean) — Indicates whether the pull request should be created as a draft. Draft pull requests cannot be merged until marked as ready for review.; `issue` (integer) — The number of an existing issue in the repository to convert into a pull request. If provided, the issue's title and body may be used for the pull request. Required if `title` is not specified.; `title` (string) — The title of the new pull request. Required unless `issue` is specified.; `head_repo` (string) — The name of the repository (e.g., 'octocat/Hello-World') where the changes in the pull request were made. This field is required for cross-repository pull requests if both the source and target repositories are owned by the same organization but are different repositories.; `maintainer_can_modify` (boolean) — Indicates whether maintainers of the upstream repository can modify the pull request. This is primarily relevant for pull requests originating from forks.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique identifier of the pull request.; `data.url` (string) — API URL for the pull request.; `data.base` (object) — Target branch information where changes will be merged.; `data.base.ref` (string) — Name of the branch.; `data.base.sha` (string) — SHA of the commit.; `data.base.repo` (object) — Repository containing the branch.; `data.base.repo.id` (integer) — Repository ID.; `data.base.repo.url` (string) — API URL.; `data.base.repo.fork` (boolean) — Whether the repository is a fork.; `data.base.repo.name` (string) — Repository name.; `data.base.repo.size` (integer) — Repository size in kilobytes.; … 498 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub Actions workflows

##### `GITHUB_GET_A_WORKFLOW` — COMPLETE

- Description: Retrieves detailed information for a specific GitHub Actions workflow in a repository, identified by either its numeric ID or its filename.
- Required inputs: `repo` (string) — Name of the repository, without the `.git` extension (case-insensitive).; `owner` (string) — Username or organization name owning the repository (case-insensitive).
- Optional inputs: `workflow_id` (integer) — Unique numeric identifier of the workflow; use this or `workflow_name`.; `workflow_name` (string) — Full name of the workflow file (e.g., `ci.yml`); use this or `workflow_id`.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — Unique numeric ID of the workflow.; `data.url` (string) — Complete API URL for this workflow resource.; `data.name` (string) — Name of the workflow (e.g., 'CI Pipeline', 'Release Drafter').; `data.path` (string) — Relative path of the workflow YAML file in the repository (e.g., '.github/workflows/main.yml').; `data.state` (string) — Current state of the workflow (e.g., `active`, `disabled_manually`, `deleted`).; `data.node_id` (string) — Globally unique identifier for the workflow object.; `data.html_url` (string) — URL to view the workflow file on GitHub.; `data.badge_url` (string) — URL for a dynamically generated status badge of the workflow.; `data.created_at` (string) — ISO 8601 timestamp of workflow creation.; `data.deleted_at` (string) — ISO 8601 timestamp of workflow deletion; present only if deleted.; `data.updated_at` (string) — ISO 8601 timestamp of last workflow update.; … 2 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_LIST_REPOSITORY_WORKFLOWS` — COMPLETE

- Description: Lists all GitHub Actions workflows for a specified repository, which must exist and be accessible.
- Required inputs: `repo` (string) — The name of the repository, without the `.git` extension. This field is not case-sensitive.; `owner` (string) — The username or organization name that owns the repository. This field is not case-sensitive.
- Optional inputs: `page` (integer) — The page number of the results to fetch.; `per_page` (integer) — The number of workflow results to return per page. Maximum value is 100.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.workflows` (array) — Array of workflow objects representing the workflows in the repository.; `data.workflows[].id` (integer) — Unique identifier for the workflow.; `data.workflows[].url` (string) — API endpoint URL for accessing this specific workflow.; `data.workflows[].name` (string) — The name of the workflow as defined in the workflow file.; `data.workflows[].path` (string) — The path to the workflow file within the repository (e.g., .github/workflows/ci.yaml).; `data.workflows[].state` (string) — Current state of the workflow. Possible values: active, deleted, disabled_fork, disabled_inactivity, disabled_manually.; `data.workflows[].node_id` (string) — GraphQL node identifier for the workflow.; `data.workflows[].html_url` (string) — GitHub web interface URL where the workflow can be viewed.; `data.workflows[].badge_url` (string) — URL for the workflow status badge image that can be embedded in documentation.; `data.workflows[].created_at` (string) — Timestamp when the workflow was created in ISO 8601 format.; `data.workflows[].deleted_at` (string) — Timestamp when the workflow was deleted in ISO 8601 format. Only present if the workflow has been deleted.; … 4 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_CREATE_A_WORKFLOW_DISPATCH_EVENT` — GENERIC

- Description: Manually triggers a GitHub Actions workflow identified by `workflow_id` at a given `ref`, if the workflow is configured to accept `workflow_dispatch` events.
- Required inputs: `ref` (string) — Git reference (branch or tag) for the workflow.; `repo` (string) — Repository name, excluding the `.git` extension (case-insensitive).; `owner` (string) — Owner of the repository (username or organization, case-insensitive).; `workflow_id` (unknown) — Identifier of the workflow: numeric ID or just the filename (e.g., 'main.yml'). If a full path like '.github/workflows/main.yml' is provided, the prefix is automatically stripped.
- Optional inputs: `inputs` (string) — JSON string containing key-value inputs for the workflow, matching `on.workflow_dispatch.inputs` definitions (max 10); uses workflow defaults if omitted. Example: '{"actor": "mona", "environment": "production"}'
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Only generic envelope fields are declared and their nested resource shape is unavailable.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub Actions workflow runs

##### `GITHUB_GET_A_WORKFLOW_RUN` — COMPLETE

- Description: Gets a specific workflow run by its ID from a GitHub repository.
- Required inputs: `repo` (string) — Name of the repository (case-insensitive, without .git extension).; `owner` (string) — The username or organization name that owns the repository (not case-sensitive).; `run_id` (integer) — Unique ID of the workflow run.
- Optional inputs: `exclude_pull_requests` (boolean) — If true, excludes pull request data; the `pull_requests` array in the response will be empty.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.id` (integer) — The unique identifier of the workflow run.; `data.url` (string) — The API URL for the workflow run.; `data.name` (string) — The name of the workflow.; `data.path` (string) — The path to the workflow file.; `data.actor` (object) — The user who triggered the workflow run.; `data.actor.id` (integer) — The unique identifier of the user.; `data.actor.url` (string) — The API URL of the user.; `data.actor.type` (string) — The type of the user (e.g., User, Organization).; `data.actor.login` (string) — The username of the user.; `data.actor.node_id` (string) — The node ID of the user.; `data.actor.html_url` (string) — The HTML URL of the user's profile.; … 152 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_LIST_WORKFLOW_RUNS_FOR_A_REPOSITORY` — COMPLETE

- Description: Lists workflow runs for a repository, allowing filtering by actor, branch, event, status, creation date, check suite ID, or head SHA; the repository must exist and be accessible.
- Required inputs: `repo` (string) — The name of the repository, without the `.git` extension (not case-sensitive).; `owner` (string) — The account owner of the repository (not case-sensitive).
- Optional inputs: `page` (integer) — Page number of the results to fetch.; `actor` (string) — Filter by the actor's login (e.g., the user who triggered the `push`).; `event` (string) — Filter by the event that triggered the workflow (e.g., `push`, `pull_request`).; `branch` (string) — Filter by branch name (e.g., `main` or the branch that triggered the `push`).; `status` (string) — Filter by `status` or `conclusion` (e.g., `success`, `in_progress`). Note: Only GitHub Actions can set `waiting`, `pending`, or `requested`.; `created` (string) — Filter by creation date or date-time range (ISO8601 format: YYYY-MM-DD or YYYY-MM-DDTHH:MM:SSZ). Supports operators like `>`, `<`, `>=`, `<=`, and `..` for ranges.; `head_sha` (string) — Filter to include only runs associated with the specified `head_sha` (commit SHA at the head of the branch).; `per_page` (integer) — Number of results per page (maximum 100).; `check_suite_id` (integer) — Filter by a specific `check_suite_id`.; `exclude_pull_requests` (boolean) — Set to `true` to exclude workflow runs triggered by pull request events. Omit or leave as null to include all runs.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.total_count` (integer) — Total number of workflow runs matching the query parameters; `data.workflow_runs` (array) — Array of workflow run objects; `data.workflow_runs[].id` (integer) — Unique identifier for the workflow run; `data.workflow_runs[].url` (string) — API URL for this workflow run; `data.workflow_runs[].name` (string) — Name of the workflow run; `data.workflow_runs[].path` (string) — Path to the workflow file in the repository; `data.workflow_runs[].actor` (object) — User who triggered the workflow run; `data.workflow_runs[].actor.id` (integer) — User ID; `data.workflow_runs[].actor.url` (string) — API URL for the user; `data.workflow_runs[].actor.type` (string) — Type of user account (e.g., User, Organization); `data.workflow_runs[].actor.login` (string) — Username of the actor; … 204 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_LIST_JOBS_FOR_A_WORKFLOW_RUN` — COMPLETE

- Description: Lists jobs for a specific workflow run in a GitHub repository.
- Required inputs: `repo` (string) — The name of the repository, without the .git extension. This field is not case-sensitive.; `owner` (string) — The username or organization name that owns the repository. This field is not case-sensitive.; `run_id` (integer) — The unique identifier of the workflow run for which jobs are to be listed.
- Optional inputs: `page` (integer) — The page number of the results to fetch. For more details, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."; `filter` (string) — Filters jobs by their `completed_at` timestamp. `latest` returns jobs from the most recent execution attempt of the workflow run. `all` returns all jobs for a workflow run, including from previous execution attempts.; `per_page` (integer) — The number of results to return per page, with a maximum of 100. For more details, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.jobs` (array) — The list of jobs for the specified workflow run.; `data.jobs[].id` (integer) — The unique identifier of the job.; `data.jobs[].url` (string) — The API URL to retrieve details about this job.; `data.jobs[].name` (string) — The name of the job.; `data.jobs[].steps` (array) — The individual steps that make up this job.; `data.jobs[].steps[].name` (string) — The name of the step.; `data.jobs[].steps[].number` (integer) — The order of the step within the job.; `data.jobs[].steps[].status` (string) — The phase of the lifecycle that the step is currently in.; `data.jobs[].steps[].conclusion` (string) — The outcome of the step after it has completed.; `data.jobs[].steps[].started_at` (string) — The time at which the step started, in ISO 8601 format.; `data.jobs[].steps[].completed_at` (string) — The time at which the step completed, in ISO 8601 format.; … 22 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub branches

##### `GITHUB_LIST_BRANCHES` — COMPLETE

- Description: List branches in a GitHub repository. Returns branch names, commit information, and protection status. Use this action when you need to retrieve all branches in a repository, optionally filtered by protection status. This is useful for identifying available branches, checking which branches are protected, or preparing to create pull requests.
- Required inputs: `repo` (string) — The name of the repository without the `.git` extension. The name is not case sensitive.; `owner` (string) — The account owner of the repository. The name is not case sensitive.
- Optional inputs: `page` (integer) — The page number of the results to fetch. For more information, see 'Using pagination in the REST API'.; `per_page` (integer) — The number of results per page (max 100). For more information, see 'Using pagination in the REST API'.; `protected` (boolean) — Setting to `true` returns only branches protected by branch protections or rulesets. When set to `false`, only unprotected branches are returned. Omitting this parameter returns all branches.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.branches` (array) — List of branches in the repository.; `data.branches[].name` (string) — The name of the branch.; `data.branches[].commit` (object) — Information about the latest commit on the branch.; `data.branches[].commit.sha` (string) — The commit SHA.; `data.branches[].commit.url` (string) — The API URL for the commit.; `data.branches[].protected` (boolean) — Whether the branch is protected.; `data.branches[].protection` (object) — Branch protection configuration. Only present if the branch is protected.; `data.branches[].protection.url` (string) — The URL for the protection configuration.; `data.branches[].protection.name` (string) — The name of the protection rule.; `data.branches[].protection.enabled` (boolean) — Whether branch protection is enabled.; `data.branches[].protection.lock_branch` (object) — Whether the branch is locked.; … 43 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo, public_repo

##### `GITHUB_GET_A_BRANCH` — COMPLETE

- Description: Retrieves detailed information for a specified branch within a GitHub repository.
- Required inputs: `repo` (string) — The name of the repository without the `.git` extension. The name is not case sensitive. ; `owner` (string) — The account owner of the repository. The name is not case sensitive.; `branch` (string) — The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql). If 'HEAD' is provided, it will be resolved to the repository's default branch.
- Optional inputs: None declared
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.name` (string) — The name of the branch; `data._links` (object) — Hypermedia links; `data._links.html` (string) — GitHub web URL for this branch; `data._links.self` (string) — API URL for this branch; `data.commit` (object) — Commit information for the branch; `data.commit.sha` (string) — SHA hash of the commit; `data.commit.url` (string) — API URL for the commit; `data.commit.author` (object) — GitHub user who authored the commit; `data.commit.author.id` (integer) — User ID; `data.commit.author.url` (string) — API URL for the user; `data.commit.author.type` (string) — Type of user account; … 114 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_CREATE_BRANCH` — COMPLETE

- Description: Creates a new branch in a GitHub repository pointing to a specified commit. Use this action when you need to create a new branch from an existing commit SHA. IMPORTANT: This action only creates NEW branches. If the branch already exists, it will fail with a 422 'Reference already exists' error. The repository must not be empty (must have at least one commit) before creating a branch.
- Required inputs: `sha` (string) — The SHA-1 hash of the commit that the new branch will point to. This must be an existing commit in the repository (40-character hexadecimal string).; `repo` (string) — The name of the repository without the .git extension. This field is not case-sensitive.; `owner` (string) — The account owner of the repository (username or organization name). This field is not case-sensitive.; `branch_name` (string) — The name of the new branch to create (e.g., 'feature-branch', 'fix-bug-123'). Do not include 'refs/heads/' prefix - just provide the branch name.
- Optional inputs: None declared
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.ref` (string) — The full name of the created reference (e.g., 'refs/heads/feature-branch').; `data.url` (string) — API URL for accessing this specific Git reference.; `data.object` (object) — Information about the Git object (commit) that the branch points to.; `data.object.sha` (string) — SHA-1 hash of the Git object (commit) that the branch points to.; `data.object.url` (string) — API URL for accessing the Git object (commit).; `data.object.type` (string) — Type of Git object, typically 'commit'.; `data.node_id` (string) — Global node identifier for the Git reference.; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub commits

##### `GITHUB_GET_A_COMMIT` — COMPLETE

- Description: Retrieves a specific commit from a repository by its owner, name, and a valid commit reference (SHA, branch, or tag), supporting pagination for large diffs.
- Required inputs: `ref` (string) — The commit reference. Can be a commit SHA (e.g., '`sha`'), a branch name (e.g., 'heads/`BRANCH_NAME`' or simply '`BRANCH_NAME`'), or a tag name (e.g., 'tags/`TAG_NAME`'). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.; `repo` (string) — The name of the repository, without the `.git` extension. This field is not case-sensitive.; `owner` (string) — The username or organization name that owns the repository. This field is not case-sensitive.
- Optional inputs: `page` (integer) — Page number for paginating the commit's diff if it's too large. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."; `per_page` (integer) — Number of results per page when paginating the commit's diff (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.sha` (string) — Commit SHA hash identifier.; `data.url` (string) — API endpoint for the commit.; `data.files` (array) — Array of file objects showing detailed changes for each modified file.; `data.files[].sha` (string) — File blob SHA hash.; `data.files[].patch` (string) — Unified diff format showing the changes.; `data.files[].status` (string) — Change status: added, removed, modified, renamed, copied, changed, or unchanged.; `data.files[].changes` (integer) — Total number of changes in the file.; `data.files[].raw_url` (string) — Raw file content URL (null for submodules).; `data.files[].blob_url` (string) — GitHub blob viewer URL for the file (null for submodules).; `data.files[].filename` (string) — File path relative to repository root.; `data.files[].additions` (integer) — Number of lines added to the file.; … 75 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo, public_repo

##### `GITHUB_LIST_COMMITS` — COMPLETE

- Description: Lists commits for a GitHub repository. Requires 'owner' (username/org) and 'repo' (repository name) parameters. Optionally filter by SHA/branch, path, author, committer, or date range.
- Required inputs: `repo` (string) — The name of the repository without the `.git` extension (repository name only, not the full 'owner/repo' path). Must contain only alphanumeric characters, hyphens (-), underscores (_), and periods (.). Wildcards like '*' are not supported.; `owner` (string) — The account owner of the repository (username or organization name only, not the full 'owner/repo' path). Must contain only alphanumeric characters and hyphens, cannot start/end with a hyphen, max 39 characters. Wildcards are not supported.
- Optional inputs: `sha` (string) — SHA hash or branch name to start listing commits from. If not provided, the GitHub API uses the repository's default branch (usually `main` or `master`).; `page` (integer) — Page number of the results to fetch when using pagination.; `path` (string) — Only commits modifying this specific file path will be returned.; `since` (string) — Only commits created on or after this timestamp will be returned. This is a timestamp in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.; `until` (string) — Only commits created before this timestamp will be returned. This is a timestamp in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.; `author` (string) — Filter commits by the commit author's GitHub login or email address.; `per_page` (integer) — Number of results to return per page (max 100).; `committer` (string) — Filter commits by the commit committer's GitHub login or email address. The committer is the user who applied the patch and may differ from the original author.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.commits` (array) — Array of commit objects from the repository.; `data.commits[].sha` (string) — SHA hash identifier of the commit.; `data.commits[].url` (string) — API endpoint URL for this commit.; `data.commits[].files` (array) — Array of files modified in the commit with detailed change information.; `data.commits[].files[].sha` (string) — SHA hash of the file blob, or null.; `data.commits[].files[].patch` (string) — Unified diff format patch showing the changes.; `data.commits[].files[].status` (string) — Type of change applied to the file: 'added', 'removed', 'modified', 'renamed', 'copied', 'changed', or 'unchanged'.; `data.commits[].files[].changes` (integer) — Total number of changes to the file.; `data.commits[].files[].raw_url` (string) — URL to access the raw file content.; `data.commits[].files[].blob_url` (string) — GitHub web interface URL to view the file blob.; `data.commits[].files[].filename` (string) — Path and name of the file.; … 75 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

##### `GITHUB_SEARCH_COMMITS` — COMPLETE

- Description: Finds commits on GitHub using a query string (q) supporting keywords and qualifiers, with options for sorting and pagination.
- Required inputs: `q` (string) — Search query with at least one text keyword plus optional qualifiers. GitHub requires actual search text (commit message keywords) - qualifiers alone are not allowed. Common qualifiers: 'repo:owner/name', 'author:username', 'committer:username', 'author-date:YYYY-MM-DD' or 'author-date:>YYYY-MM-DD..YYYY-MM-DD', 'committer-date:YYYY-MM-DD', 'merge:true/false', 'hash:HASH', 'parent:HASH', 'tree:HASH'. IMPORTANT: The 'hash:' qualifier requires a full 40-character hexadecimal commit hash (e.g., 'hash:e5bd3914e2e596debea16f433f57875b5b90bcd6'). Do NOT use branch names, tag names, or short refs with 'hash:' - use 'repo:owner/name' with other filters to search commits on a specific branch.
- Optional inputs: `page` (integer) — Page number of results to retrieve.; `sort` (string) — Sorts results by 'author-date' or 'committer-date'. Use separately with the 'order' parameter (e.g., sort='committer-date' with order='desc'). Do not combine them into a single value. If unspecified, GitHub defaults to 'best match' ranking.; `order` (string) — Sort order: 'desc' (newest first) or 'asc' (oldest first). Use with the 'sort' parameter to control direction (e.g., sort='committer-date' with order='desc').; `per_page` (integer) — Number of results per page (maximum 100).
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.items` (array) — Array of commit search results; `data.items[].sha` (string) — Commit SHA hash; `data.items[].url` (string) — API URL for the commit; `data.items[].score` (number) — Search relevance score for the commit; `data.items[].author` (object) — GitHub user account of the commit author; `data.items[].author.id` (integer) — Unique identifier for the user; `data.items[].author.url` (string) — API URL for the user; `data.items[].author.type` (string) — Type of user account (e.g., User, Organization); `data.items[].author.login` (string) — GitHub username; `data.items[].author.node_id` (string) — GraphQL node identifier; `data.items[].author.html_url` (string) — GitHub web URL for the user profile; … 148 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: repo

#### GitHub repository contents

##### `GITHUB_GET_REPOSITORY_CONTENT` — COMPLETE

- Description: Retrieves a file's Base64 encoded content or lists a directory's contents from a GitHub repository.
- Required inputs: `path` (string) — Required. Path to file or directory in the repository. Empty string ('') retrieves root directory contents. Leading slashes auto-stripped. CRITICAL: Do NOT pass branch names here (e.g., 'feat/godlocal-dev', 'main'). Branch names belong in the `ref` parameter. Path should be file/directory path only (e.g., 'src/main.js', 'README.md'), NOT prefixed with branch name (use 'src/main.js' not 'main/src/main.js'). File paths must include extension (e.g., 'README.md', not 'README'). For common files (README, LICENSE, CHANGELOG) without extensions, action auto-tries common extensions (.md, .txt, .rst). Paths are case-sensitive. On 404 errors, list parent directory to verify exact path/casing.; `repo` (string) — Required. The name of the repository, without the `.git` extension. This name is not case sensitive.; `owner` (string) — Required. The account owner of the repository. This name is not case sensitive. Can also accept full repository path in format 'owner/repo' - the repo part will be extracted automatically.
- Optional inputs: `ref` (string) — The name of the commit, branch, or tag to read from. Use this parameter to specify which branch (e.g., 'feat/godlocal-dev', 'main', 'develop'), tag (e.g., 'v1.2.0'), or commit SHA you want to access. If not provided, the repository's default branch will be used. Note: Branch names should be passed here, NOT in the path parameter.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.content` (unknown) — Single object for files/symlinks/submodules, or array of objects for directories; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: public_repo, repo, workflow

##### `GITHUB_GET_RAW_REPOSITORY_CONTENT` — COMPLETE

- Description: Tool to fetch raw file content from a GitHub repository. Returns raw bytes in whatever format the file uses (JSON, CSV, binary, etc.) — parsing and validation are the caller's responsibility. Ensure owner, repo, ref, and path are consistent when combining with other GitHub tools to avoid mismatched-ref errors.
- Required inputs: `path` (string) — The file path within the repository, relative to the repository root. Use forward slashes for directory separators (e.g., 'src/utils/helper.js'). Do not include leading slash. Do NOT pass a full GitHub URL - only the relative path within the repo. Must exactly match the file path as it exists in the repository — use GITHUB_GET_A_TREE to discover valid paths rather than guessing directory structure.; `repo` (string) — The repository name (not the full URL). This is the repository name shown in the repository URL (e.g., for github.com/torvalds/linux, the repo is 'linux'). Do not include '.git' suffix. Case-insensitive.; `owner` (string) — The GitHub username or organization name that owns the repository. This is the account name shown in the repository URL (e.g., for github.com/torvalds/linux, the owner is 'torvalds'). Case-insensitive.
- Optional inputs: `ref` (string) — The branch name, tag name, or commit SHA to fetch the file from. Defaults to the repository's default branch (usually 'main' or 'master'). Recommended to pass explicitly — omitting it fetches from the default branch, which may not match the ref used in other tool calls and can yield unexpected or missing content.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.content` (object) — Raw file content of the requested repository file.; `data.content.name` (string) — Name of the file; `data.content.s3url` (string) — S3 URL of the downloaded file.; `data.content.mimetype` (string) — Mime type of the file.; `error` (string) — Error if any occurred during the execution of the action; `successful` (boolean) — Whether or not the action execution was successful or not
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: none returned

##### `GITHUB_CREATE_OR_UPDATE_FILE_CONTENTS` — COMPLETE

- Description: Creates a new file or updates an existing file in a GitHub repository; SHA can be provided to validate file updates, automatically fetched if not provided.
- Required inputs: `path` (string) — The full path to the file in the repository, including the filename and extension. Do not include leading slashes.; `repo` (string) — The name of the repository (not case sensitive, without the `.git` extension).; `owner` (string) — The account owner of the repository (not case sensitive).; `content` (string) — The new file content. Can be provided as plain text (will be automatically encoded to Base64) or as Base64-encoded text. GitHub API requires Base64 encoding, which this action handles automatically.; `message` (string) — The commit message for this file creation or update.
- Optional inputs: `sha` (string) — The blob SHA of the file being replaced. Automatically fetched if not provided. If a 409 conflict occurs (SHA mismatch due to concurrent modifications), the action will automatically retry up to 5 times with exponential backoff and jitter.; `branch` (string) — The branch name. Defaults to the repository's default branch if omitted. If a non-existent branch is specified, the action will automatically fall back to the repository's default branch. For empty repositories, omitting this parameter is recommended to let GitHub initialize with the repository's configured default branch.; `author__date` (string) — Author's timestamp (ISO 8601 format). Defaults to committer's date if author details are provided but date is not.; `author__name` (string) — Author's name. If specified, `author_email` is also required. Defaults to committer details; if all committer/author details omitted, GitHub uses authenticated user.; `author__email` (string) — Author's email. If specified, `author_name` is also required. Defaults to committer details; if all committer/author details omitted, GitHub uses authenticated user.; `committer__date` (string) — Committer's commit timestamp (ISO 8601 format). Defaults to current time if committer details are provided but date is not.; `committer__name` (string) — Committer's name. If specified, `committer_email` is also required. GitHub uses authenticated user if all committer/author details omitted.; `committer__email` (string) — Committer's email. If specified, `committer_name` is also required. GitHub uses authenticated user if all committer/author details omitted.
- Nested inputs: None declared
- Output fields: `data` (object) — Data from the action execution; `data.commit` (object) — Information about the commit including sha, html_url, author, committer, message, and tree.; `data.commit.sha` (string) — SHA of the commit; `data.commit.url` (string) — API URL for the commit; `data.commit.tree` (object) — Tree information for the commit; `data.commit.tree.sha` (string) — SHA of the tree; `data.commit.tree.url` (string) — API URL for the tree object; `data.commit.author` (object) — Author of the commit; `data.commit.author.date` (string) — Timestamp of the commit; `data.commit.author.name` (string) — Name of the author or committer; `data.commit.author.email` (string) — Email of the author or committer; `data.commit.message` (string) — Commit message; … 33 more
- Output assessment: Named resource fields are present with mostly typed and described structure.
- Version: 20260713_00; available versions: 20260713_00
- Deprecated: no; scopes: public_repo, repo, workflow

## Inconsistencies affecting dependency extraction

### googlesuper

- Deprecated tools: 38.
- Tools with REST/GraphQL indicators: 0.
- Tools exposing generic output fields named `id` or `number`: 152.
- Tools with at least one output field lacking a description: 46.
- Tools flagged as unsafe for automatic dependency inference without additional evidence: 57.
- Google Super combines multiple services with different identifier conventions and schema depth:
  - other: 194
  - drive: 65
  - calendar: 44
  - gmail: 40
  - docs: 37
  - analytics: 28
  - tasks: 21
  - sheets: 20
  - meet: 8
  - slides: 7
  - people/contacts: 3

### github

- Deprecated tools: 22.
- Tools with REST/GraphQL indicators: 50.
- Tools exposing generic output fields named `id` or `number`: 447.
- Tools with at least one output field lacking a description: 11.
- Tools flagged as unsafe for automatic dependency inference without additional evidence: 173.
- GitHub includes high-level convenience actions and low-level Git object operations; repository file actions can overlap with blob/tree/commit/reference workflows.
- GitHub also contains scope-specific variants for users, repositories, organizations, teams, environments, and authenticated-user resources.

## Schemas that cannot safely support dependency inference

### googlesuper: 57 flagged tools

- `GOOGLESUPER_ACL_WATCH`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_ARCHIVE_CUSTOM_DIMENSION`: Output shape is too generic to infer identifier producers safely. Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_CALENDAR_LIST_WATCH`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_CHANNELS_STOP`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_CLEAR_TASKS`: Output shape is too generic to infer identifier producers safely.
- `GOOGLESUPER_CREATE_DRIVE`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_CREATE_FILE`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_CREATE_FOLDER`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_CREATE_NAMED_RANGE`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_CREATE_SHORTCUT_TO_FILE`: Ambiguous required input names without enough resource context: name. Generic output id/number fields lack descriptions.
- `GOOGLESUPER_CREATE_TEAM_DRIVE`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_DELETE_FILE`: Output shape is too generic to infer identifier producers safely.
- `GOOGLESUPER_DELETE_TASK`: Output shape is too generic to infer identifier producers safely.
- `GOOGLESUPER_DELETE_TASK_LIST`: Output shape is too generic to infer identifier producers safely.
- `GOOGLESUPER_EDIT_FILE`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_EVENTS_LIST_ALL_CALENDARS`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_EVENTS_WATCH`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_GET_ACCOUNT`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_ATTRIBUTION_SETTINGS`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_AUDIENCE_EXPORT`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_AUDIENCE_LIST`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_CAMPAIGN_BY_ID`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_GET_CAMPAIGN_BY_NAME`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_CONFERENCE_RECORD_BY_NAME`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_CUSTOM_DIMENSION`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_DATA_RETENTION_SETTINGS`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_DATA_SHARING_SETTINGS`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_FILE_METADATA`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_GET_FILE_V2`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_GET_FILTER`: Ambiguous required input names without enough resource context: id.
- `GOOGLESUPER_GET_GOOGLE_SIGNALS_SETTINGS`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_METADATA`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_PARTICIPANT_SESSION`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_PLACE_DETAILS`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_PROPERTY`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_RECURRING_AUDIENCE_LIST`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_TRANSCRIPT`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_GET_TRANSCRIPT_ENTRY`: Ambiguous required input names without enough resource context: name.
- `GOOGLESUPER_LIST_CHANGES`: Generic output id/number fields lack descriptions.
- `GOOGLESUPER_LIST_PERMISSIONS`: Generic output id/number fields lack descriptions.
- … 17 additional flagged tools are present in the raw data.

### github: 173 flagged tools

- `GITHUB_ACCEPT_REPOSITORY_INVITATION`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_FIELD_TO_USER_PROJECT`: Ambiguous required input names without enough resource context: name.
- `GITHUB_ADD_OR_UPDATE_TEAM_REPOSITORY_PERMISSIONS`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_PROJECT_COLLABORATOR`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_REPOSITORY_TO_APP_INSTALLATION`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_VISIBILITY`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_SECRET`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_VARIABLE`: Output shape is too generic to infer identifier producers safely. Ambiguous required input names without enough resource context: name.
- `GITHUB_ADD_SELECTED_REPOSITORY_TO_USER_SECRET`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_APPROVE_WORKFLOW_RUN_FOR_FORK_PULL_REQUEST`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_ASSIGN_ORGANIZATION_ROLE_TO_TEAM`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_BLOCK_USER`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CANCEL_GITHUB_PAGES_DEPLOYMENT`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CHECK_IF_USER_CAN_BE_ASSIGNED`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CHECK_IF_USER_IS_BLOCKED_BY_AUTHENTICATED_USER`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CONFIGURE_OIDC_SUBJECT_CLAIM_TEMPLATE`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_A_CHECK_RUN`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_CUSTOM_ORGANIZATION_ROLE`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_DEPLOY_KEY`: Ambiguous required input names without enough resource context: key.
- `GITHUB_CREATE_A_LABEL`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_AN_ENVIRONMENT_VARIABLE`: Output shape is too generic to infer identifier producers safely. Ambiguous required input names without enough resource context: name, value.
- `GITHUB_CREATE_AN_ORGANIZATION_PROJECT`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_AN_ORGANIZATION_VARIABLE`: Output shape is too generic to infer identifier producers safely. Ambiguous required input names without enough resource context: name, value.
- `GITHUB_CREATE_A_PROJECT_COLUMN`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_REFERENCE`: Ambiguous required input names without enough resource context: ref.
- `GITHUB_CREATE_A_REPOSITORY_DISPATCH_EVENT`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_A_REPOSITORY_PROJECT`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_REPOSITORY_RULESET`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_REPOSITORY_VARIABLE`: Output shape is too generic to infer identifier producers safely. Ambiguous required input names without enough resource context: name, value.
- `GITHUB_CREATE_A_TEAM`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_USER_PROJECT`: Ambiguous required input names without enough resource context: name.
- `GITHUB_CREATE_A_WORKFLOW_DISPATCH_EVENT`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_OR_UPDATE_AN_ENVIRONMENT_SECRET`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_OR_UPDATE_AN_ORGANIZATION_SECRET`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_OR_UPDATE_A_REPOSITORY_SECRET`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_OR_UPDATE_A_SECRET_FOR_THE_AUTHENTICATED_USER`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_UPDATE_ORG_SECRET_WITH_LIB_SODIUM`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_CREATE_VIEW_FOR_USER_PROJECT`: Ambiguous required input names without enough resource context: name.
- `GITHUB_DECLINE_A_REPOSITORY_INVITATION`: Output shape is too generic to infer identifier producers safely.
- `GITHUB_DELETE_ACCESS_RESTRICTIONS`: Output shape is too generic to infer identifier producers safely.
- … 133 additional flagged tools are present in the raw data.

## Important limitations

- A declared output path proves only that the schema exposes the field; it does not prove that every successful execution populates it.
- Generic wrappers such as `data`, `successful`, and `error` are not themselves useful dependency entities unless `$ref`/`$defs` expose the nested resource shape.
- Fields named `id`, `number`, `name`, `path`, or `ref` must not be matched across tools without service, resource, and scope evidence.
- Deprecated tools, REST/GraphQL variants, and convenience/low-level overlaps should be resolved deliberately in the next phase rather than treated as equivalent.
- This phase does not normalize schemas, infer dependencies, call an LLM, plan workflows, or build a visualization.

